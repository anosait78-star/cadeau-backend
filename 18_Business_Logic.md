# 18 — BUSINESS LOGIC
## توثيق منطق الأعمال والقواعد والحسابات الكامل لنظام OrdersFlow

> **حالة الوثيقة:** مرجع رسمي معتمد (Authoritative Reference)
> **نطاق هذا الملف:** التوثيق الشامل لكل قواعد الأعمال، المعادلات المالية والمخزنية، منطق التحقق، القيم الثابتة ودلالاتها، والاشتقاقات — كمرجع تنفيذي موحّد.
> **الطبيعة:** ملف **وصفي** (يوثّق منطق OrdersFlow كما هو). الاقتراحات في `21`.
> **تصنيف الثقة:** ✅ مؤكد · 🟡 مرجّح · ⚪ تخمين
> **هذا الملف مستقل بذاته** — يُقرأ دون الرجوع لأي ملف آخر.
> **مرجع مكمّل:** سير العمل في `14_Workflows.md`، البيانات في `11_Database.md`.

---

# Overview

هذا الملف هو **المرجع الموحّد لكل منطق الأعمال** في OrdersFlow — القواعد التي تحكم كيف تُحسب القيم، ما المسموح وما الممنوع، وكيف تُشتقّ الحالات. بينما تصف ملفات الشاشات "ماذا يرى المستخدم"، يصف هذا الملف **"لماذا وكيف يتصرّف النظام"** رياضيًا ومنطقيًا.

المنطق يتمحور حول **خصوصية نموذج COD**: المبلغ المستلم فعليًا أقل من قيمة الطلب (رسوم الشحن)، والربحية تُحسب على المُحصّل لا الإجمالي. هذا القرار يتخلّل كل الحسابات المالية.

**التصنيف:** مواصفات منطق أعمال (Business Rules Specification).

---

# القسم أ: منطق الطلبات (Order Logic)

## أ.1 — حساب إجمالي الطلب ✅/🟡
```
subtotal = Σ (order_items.price × order_items.quantity)     [مجموع البنود]
total = subtotal + shipping_cost − discount                  🟡
```
- `subtotal`: مجموع بنود الطلب. ✅
- `shipping_cost`: تكلفة الشحن (قد تُشتقّ من `shipping_zones` حسب المحافظة) 🟡.
- `discount`: خصم (يظهر بالسالب في الجدول). ✅
- **الثقة:** التركيبة مرجّحة 🟡 (مرصودة القيم لا المعادلة الصريحة).

## أ.2 — المبلغ المحصّل (Collected Amount) — القاعدة المحورية ✅
```
collected_amount = المبلغ الفعلي المستلم من العميل بعد خصم رسوم شركة الشحن
```
- **ليس** `total` و**ليس** `total − shipping_cost` بالضرورة.
- **الدلالة:** في COD، شركة الشحن تحصّل من العميل وتخصم رسومها قبل تحويل الباقي للتاجر.
- **الأهمية:** أساس كل حسابات الربحية والمركز النقدي.
- **النص المرصود (✅):** "المبلغ الفعلي المستلم بعد خصم رسوم شركة الشحن".

## أ.3 — بُعدا الحالة ✅
- **`status`** (12 قيمة): مرحلة الطلب في دورة الحياة.
- **`follow_up_status`**: حالة المتابعة (قيد الانتظار…) — **مستقلة تمامًا**.
- **القاعدة:** الطلب يحمل الحالتين معًا؛ لا تعتمد إحداهما على الأخرى.

## أ.4 — منطق الإلغاء ✅
```
الإلغاء يتطلب سببًا من order_reasons (type = cancel)
cancel_reason_id → order_reasons.id
```
- الأسباب مخصّصة لكل شركة (لم يُدفع العربون/العميل لا يرد/تيست/متكرر…).
- **تغذّي التحليلات** (تحليل المنتجات الملغاة بالأسباب).

## أ.5 — منطق التصنيفات (M:N) ✅
- طلب → عدة تصنيفات؛ تصنيف → عدة طلبات (عبر `order_label_assignments`).
- كل تصنيف بلون (`order_labels.color`).
- لا قيود منطقية على التصنيفات (وسوم حرة).

---

# القسم ب: منطق المخزون (Inventory Logic)

## ب.1 — المخزون المتاح ✅/🟡
```
المتاح (Available) = on_hand − committed
```
- `on_hand`: الكمية الفيزيائية في المستودع. ✅
- `committed`: المحجوز لطلبات قيد المعالجة. ✅
- **القاعدة:** البيع يعتمد على المتاح لا `on_hand` الكامل 🟡.

## ب.2 — حجز وتحرير المخزون 🟡
```
طلب → قيد المعالجة:  committed += كمية البنود
طلب → تم الشحن:      on_hand −= الكمية · committed −= الكمية
طلب → ملغي:          committed −= الكمية (تحرير)
طلب → مرتجع:         on_hand += الكمية (إرجاع)
```
- **الثقة:** الحقول مرصودة ✅، التوقيت الدقيق مرجّح 🟡.

## ب.3 — تنبيه المخزون المنخفض ✅/🟡
```
تنبيه إذا:  on_hand ≤ low_stock_threshold   (لكل زوج منتج×مستودع)
```
- يظهر في لوحة التحكم (548 عنصرًا في المثال).

## ب.4 — السماح بالبيع عند النفاد ✅
```
if allow_oversell = true:  يُسمح بطلب حتى لو on_hand = 0 (بيع مسبق)
else:  يُمنع/يُحذّر عند النفاد
```

## ب.5 — منطق المتغيرات ✅
```
المتغير = صف products بـ parent_product_id ≠ NULL
كل متغير: سعره، تكلفته، مخزونه، SKU مستقلة
```
- المنتج الأب: `parent_product_id = NULL`.

## ب.6 — منطق التحويل (ذري) ✅
```
تحويل: from_inventory.on_hand −= كمية · to_inventory.on_hand += كمية
       + إنشاء stock_transfers
لا يتغيّر إجمالي المخزون عبر الشركة، فقط توزيعه
القيد: كمية التحويل ≤ المتاح في المصدر
```

---

# القسم ج: منطق المشتريات (Procurement Logic)

## ج.1 — حالة دفع أمر الشراء (مشتقّة) 🟡
```
paid = Σ po_payments.amount
if paid = 0:              غير مدفوع
if 0 < paid < total:      مدفوع جزئياً
if paid ≥ total_amount:   مدفوع بالكامل
```

## ج.2 — حالة استلام أمر الشراء (مشتقّة) 🟡
```
received = Σ po_receiving_logs.quantity
ordered = Σ po_items.quantity
if received = 0:            لم يُستلم
if 0 < received < ordered:  جزئي
if received ≥ ordered:      مستلم بالكامل
```

## ج.3 — أثر الاستلام على المخزون 🟡
```
تسجيل po_receiving_logs → inventory_stock.on_hand += الكمية المستلمة
                          (في المستودع المستهدف)
```

## ج.4 — رصيد المورد ✅/🟡
```
balance/remaining = Σ (أوامر الشراء) − Σ (المدفوعات)
                  = المستحق للمورد
```

---

# القسم د: المنطق المالي (Financial Logic) — المعادلات المحورية

## د.1 — صافي الدخل (Net Income) ✅ — أهم معادلة
```
صافي الدخل = collected_amount − إجمالي المصروفات − تكلفة البضائع المباعة (COGS)
Net Income = Σ collected_amount − Σ expenses − COGS
```
- **يُحسب على المُحصّل** (`collected_amount`) لا الإجمالي (`total`) — خصوصية COD. ✅
- **مؤكد** من شاشة التحليلات (~57,467 ج.م. في المثال).

## د.2 — تكلفة البضائع المباعة (COGS) ✅
```
COGS = Σ (products.cost × الكمية المباعة)
```
- على الطلبات المباعة/المكتملة.

## د.3 — المركز النقدي (Cash Position) ✅
```
النقد في اليد = Σ collected_amount − Σ po_payments − Σ expenses
Cash on Hand = المحصّل − المدفوع للموردين − المصروفات

المستحق للموردين = Σ (أرصدة الموردين المتبقية)
```

## د.4 — قيمة المخزون (Inventory Value) ✅
```
قيمة المخزون (بالتكلفة) = Σ (products.cost × inventory_stock.on_hand)
                          لكل المنتجات في كل المستودعات
```

## د.5 — صافي الربح الشهري (Monthly P&L) ✅
```
صافي الربح = الإيرادات − تكلفة البضائع − الإعلانات − المصروفات (الأخرى)
```
- الإعلانات فئة مصروف مميّزة في P&L.

## د.6 — رأس المال الختامي والترحيل ✅/🟡
```
closing_capital = رأس المال الافتتاحي + صافي ربح الشهر  🟡
الشهر التالي: افتتاحي = closing_capital السابق (ترحيل)
```

## د.7 — قواعد الإقفال الشهري ✅
```
شروط الإقفال (كلها إلزامية):
  1. لا طلبات مفتوحة (كلها محدّثة)
  2. كل أوامر الشراء مستلمة
  3. كل مصروفات الشهر مسجّلة
قيد التسلسل: لا يُقفل شهر قبل إقفال سابقه
قيد القفل: الشهر المقفل لا يقبل تعديلًا بأثر رجعي
```

## د.8 — غياب الضرائب ✅
```
لا VAT/ضريبة في أي معادلة — كل الحسابات صافية بلا ضريبة
```

---

# القسم هـ: منطق العملاء (Customer Logic)

## هـ.1 — تعريف العميل ✅/🟡
```
العميل يُعرَّف بالهاتف (phone) ضمن الشركة (مرجّح مفتاح تجميع)
مرجّح index فريد (company_id, phone)
```

## هـ.2 — مؤشرات العميل ✅/🟡
```
total_orders = عدد طلبات العميل
total_revenue = Σ قيم طلبات العميل
متوسط قيمة الطلب = total_revenue ÷ total_orders
```
- مخزّنة أو محسوبة (غير محسوم) 🟡.

## هـ.3 — إنشاء العميل التلقائي 🟡
```
عند إنشاء طلب بهاتف جديد → إنشاء عميل تلقائيًا (مرجّح)
عند هاتف موجود → ربط بالعميل الحالي
```

## هـ.4 — تصنيف العائد/الجديد 🟡
```
عميل عائد = له أكثر من طلب
عميل جديد = طلبه الأول في الفترة
العائدون % = (عائدون ÷ إجمالي العملاء) × 100
```

---

# القسم و: منطق الموظفين والأداء (Employee Logic)

## و.1 — معدل التحويل ✅/🟡
```
معدل التحويل = (طلبات مكتملة للعضو ÷ إجمالي طلباته) × 100  🟡
```

## و.2 — مؤشرات النشاط ✅
```
لكل عضو: أنشأ · مُسند إليه · تم التسليم · مكتمل · ملغي · مرتجع
الإيراد المُحقّق = Σ قيم طلباته المكتملة
```

## و.3 — الإسناد ✅
```
assigned_to → company_members
الإسناد يُطلق إشعار Web Push للعضو
```

---

# القسم ز: القيم الثابتة ودلالاتها (Enums & Semantics)

## حالات الطلب (12) — الدلالة الكاملة ✅
| الحالة | الدلالة | المرحلة |
|-------|---------|---------|
| **جديد** | مُستقبل لم يُؤكّد | الاستقبال |
| **جاري التأكيد** | قيد التأكيد الهاتفي/واتساب | التأكيد |
| **قيد المعالجة** | مؤكّد، قيد التجهيز (حجز مخزون) | التجهيز |
| **ناقص** | ينقص صنف لإكماله | التجهيز |
| **جاهز** | جُهّز، بانتظار الشحن | التجهيز |
| **تم الشحن** | سُلّم لشركة الشحن (بوليصة) | الشحن |
| **تم التسليم** | وصل للعميل | التسليم |
| **مكتمل** | سُلّم + حُصّل (إغلاق) | الإغلاق |
| **مؤجل** | مؤجّل لإعادة جدولة | فرعي |
| **ملغي** | ألغي (بسبب) | فرعي |
| **مرتجع** | أُرجع بعد الشحن/التسليم | فرعي |
| **مستبدل** | استُبدل بمنتج آخر | فرعي |

## حالة الدفع (4) ✅
`غير مدفوع` (افتراضي) · `مدفوع جزئياً` · `مدفوع مسبقاً` (عربون) · `مدفوع بالكامل`.

## حالة المتابعة 🟡
`قيد الانتظار` (+ قيم أخرى غير مرصودة).

## فئات المصروفات (6) ✅
`الرواتب` · `الصيانة` · `المواد` · `الأصول` · `إعلانات` · `أخرى`.

## حالة استلام PO (3) ✅
`لم يُستلم` · `جزئي` · `مستلم بالكامل`.

## نوع العميل ✅/🟡
`Regular` (+ أنواع أخرى مرجّحة).

## نوع المورد 🟡
`محلي` (+ `مستورد`).

## نوع السبب ✅
`cancel` · `return`.

---

# القسم ح: قواعد التحقق (Validation Rules) — موحّدة

| الكيان | الحقل | القاعدة | الثقة |
|-------|------|---------|------|
| **طلب** | الهاتف/الاسم/المحافظة | إلزامية (نجمة) | 🟡 |
| طلب | منتج واحد على الأقل | إلزامي | 🟡 |
| طلب | الكمية | > 0 | ⚪ |
| طلب | الخصم | ≤ subtotal | ⚪ |
| **منتج** | الاسم/السعر | إلزاميان | ✅ |
| منتج | السعر/التكلفة/المخزون | ≥ 0 | 🟡 |
| منتج | SKU | فريد ضمن الشركة (مرجّح) | 🟡 |
| **متغير** | الاسم/السعر | إلزاميان | ✅ |
| **عميل** | الهاتف | إلزامي + فريد (مرجّح) | 🟡 |
| **مصروف** | المبلغ | > 0 | 🟡 |
| مصروف | الفئة | من القيم المحدّدة | ✅ |
| **PO** | الدفعة | ≤ المتبقي | 🟡 |
| PO | الاستلام | ≤ المطلوب (تراكميًا) | 🟡 |
| **تحويل** | الكمية | ≤ المتاح بالمصدر | 🟡 |
| تحويل | المصدر ≠ الهدف | إلزامي | ⚪ |
| **إقفال** | التسلسل + الشروط | إلزامية | ✅ |
| **مستودع** | افتراضي واحد | لكل شركة | 🟡 |

---

# القسم ط: القيم الافتراضية (Defaults) ✅

| الكيان | الحقل | الافتراضي |
|-------|------|-----------|
| طلب | المستودع | المستودع الرئيسي (is_default) |
| طلب | حالة الدفع | غير مدفوع |
| طلب | الحالة | جديد |
| عميل | النوع | Regular |
| عميل | الحالة | نشط |
| منتج | allow_oversell | مرجّح false |
| مصروف | التاريخ | اليوم (مرجّح) |
| النظام | اللغة | حسب الإعداد |
| النظام | الثيم | حسب localStorage |
| النظام | بادئة الدولة | 20 (مصر) |

---

# القسم ي: الاشتقاقات (Derivations) — مشتقّ مقابل مخزّن

| القيمة | مشتقّة/مخزّنة (مرجّح) | المصدر | الثقة |
|-------|----------------------|--------|------|
| `orders.total` | مخزّنة | subtotal + shipping − discount | 🟡 |
| `orders.subtotal` | مخزّنة/محسوبة | Σ البنود | 🟡 |
| `customers.total_orders/revenue` | مخزّنة (Trigger) | طلبات العميل | 🟡 |
| `po.payment_status` | محسوبة | Σ المدفوعات | 🟡 |
| `po.receiving_status` | محسوبة | Σ الاستلامات | 🟡 |
| `inventory_stock.committed` | مخزّنة | حالة الطلبات | 🟡 |
| مؤشرات التحليلات | محسوبة لحظيًا (RPC) | تجميعات | ✅ |
| المركز النقدي/P&L | محسوبة (RPC) | تجميعات | ✅ |
| رصيد المورد | محسوبة/مخزّنة | أوامر − مدفوعات | 🟡 |

> **قرار إعادة البناء (⚪):** حسم كل قيمة (مخزّنة للأداء أم محسوبة للدقة) — أهم قرار في نموذج البيانات.

---

# Edge Cases (منطقية)

| الحالة | القاعدة المتوقّعة | الثقة |
|-------|-------------------|------|
| خصم > subtotal | يجب منعه (total سالب) | ⚪ |
| تحصيل > total | نادر، يحتاج تحقق/تنبيه | ⚪ |
| بيع آخر قطعة متزامن | قفل يمنع oversell غير مقصود | ⚪ |
| منتج بتكلفة 0 | COGS مضلّل (ربح مبالغ) | 🟡 |
| هاتف بصيغ مختلفة | خطر تكرار العميل | ⚪ |
| إرجاع بعد الإكمال | عكس مالي/مخزوني معقّد | 🟡 |
| استلام PO زائد | تحقق/تحذير | 🟡 |
| إقفال بطلبات مؤجلة | تعامل قائمة التحقق | 🟡 |

---

# Failure Scenarios (منطقية)

- **حساب صافي دخل خاطئ** بسبب تكلفة صفرية/محصّل غير مسجّل → قرارات مبنية على أرقام خاطئة ⚪.
- **مخزون غير متسق** بسبب فشل أثر جانبي غير ذري → بيع ما لا يوجد ⚪.
- **تكرار العملاء** بسبب هواتف غير موحّدة → CRM مشوّه ⚪.
- **إقفال جزئي** → غموض محاسبي 🟡.

---

# Technical Notes

- **`collected_amount` هو حجر الزاوية المالي** — كل الربحية تُبنى عليه. أي إعادة بناء يجب أن تجعله مركزيًا.
- **الاشتقاقات (مشتقّ/مخزّن)** أهم قرار في نموذج البيانات — يوازن الأداء والدقة.
- **الآثار الجانبية للمخزون** يجب أن تكون ذرية (Triggers/RPC).
- **غياب الضرائب** فجوة للامتثال — قرار تصميمي واعٍ (سوق COD غير رسمي غالبًا) لكنه يحدّ التوسّع للأسواق الرسمية.
- **الإقفال المتسلسل القافل** ضمانة محاسبية قوية.

---

# Reverse Engineering Notes

- **المعادلات المالية المحورية (صافي الدخل/المركز النقدي/قيمة المخزون) مؤكدة ✅** من شاشة التحليلات (القيم + التعريفات المعروضة).
- **`collected_amount` ودلالته مؤكد ✅** (النص الصريح في الواجهة).
- **معادلة total مرجّحة 🟡** (القيم مرصودة لا الصيغة).
- **حالات PO المشتقّة مرجّحة 🟡** (منطقية من الحقول).
- **الاشتقاقات (مشتقّ/مخزّن) مرجّحة/تخمين** — لم تُرصد Triggers.
- **قواعد التحقق الدقيقة مرجّحة 🟡** (النجوم مرصودة، القيود الخادمية لا).
- **صيغ معدل التحويل/العائدون مرجّحة 🟡** (التعريف منطقي).

---

# Confidence Level

| العنصر | الثقة |
|-------|------|
| صافي الدخل (على المحصّل) | ✅ مؤكد |
| collected_amount ودلالته | ✅ مؤكد |
| المركز النقدي وقيمة المخزون | ✅ مؤكد |
| P&L الشهري | ✅ مؤكد |
| قواعد الإقفال (تسلسل/شروط/قفل) | ✅ مؤكد |
| دلالات حالات الطلب والدفع | ✅ مؤكد |
| فئات المصروفات وحالات PO | ✅ مؤكد |
| معادلة total | 🟡 مرجّح |
| منطق حجز/تحرير المخزون | 🟡 مرجّح |
| الاشتقاقات (مشتقّ/مخزّن) | 🟡/⚪ |
| قواعد التحقق الدقيقة | 🟡 مرجّح |
| صيغ الأداء (تحويل/عائدون) | 🟡 مرجّح |

---

READY FOR NEXT FILE
