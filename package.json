{
  "name": "cadeau-backend",
  "version": "0.0.0",
  "private": true,
  "description": "Cadeau CRM backend — NestJS API + internal packages (config, crypto, database).",
  "packageManager": "pnpm@10.15.0",
  "engines": {
    "node": ">=24.0.0 <25.0.0",
    "pnpm": ">=10.0.0"
  },
  "scripts": {
    "build": "turbo run build",
    "dev": "pnpm --filter @cadeau/api dev",
    "type-check": "turbo run type-check",
    "test": "turbo run test"
  },
  "devDependencies": {
    "turbo": "2.10.7",
    "typescript": "5.7.2"
  }
}
