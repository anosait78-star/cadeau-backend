# 06 — INVENTORY (PRODUCTS)
## التوثيق الهندسي الكامل لوحدة المنتجات والمخزون (`/inventory`)

> **حالة الوثيقة:** مرجع رسمي معتمد (Authoritative Reference)
> **نطاق هذا الملف:** تحليل وحدة المنتجات والمخزون والمتغيرات عنصرًا عنصرًا — الكتالوج، المخزون لكل مستودع، المتغيرات (لون/مقاس/عطر)، البيانات، الـ APIs، والقواعد.
> **المسار:** `/inventory`
> **الشركة المرجعية:** Cadeau Egypt (564 منتجًا)
> **تصنيف الثقة:** ✅ مؤكد · 🟡 مرجّح · ⚪ تخمين
> **هذا الملف مستقل بذاته** — يُقرأ دون الرجوع لأي ملف آخر.
> **ملاحظة تسمية مهمة:** "المستودعات" تُخزَّن في جدول اسمه `inventories` (لا `warehouses`)، و"المخزون الفعلي" في `inventory_stock`. تفاصيل المستودعات في `07_Warehouses.md`؛ هذا الملف يركّز على **المنتجات ومخزونها**.

---

# Overview

وحدة المنتجات/المخزون (Inventory) هي **كتالوج منتجات الشركة + نظام تتبّع المخزون**. تحتوي في Cadeau Egypt على **564 منتجًا**. الوحدة تدير:

1. **الكتالوج:** تعريف المنتجات (اسم، SKU، سعر، تكلفة، صورة).
2. **المخزون لكل مستودع:** كمية كل منتج داخل كل مستودع (`on_hand`, `committed`).
3. **المتغيرات (Variants):** نسخ المنتج (لون/مقاس/عطر) كأبناء للمنتج الأب، لكل منها سعرها ومخزونها.

الوحدة محورية لثلاث عمليات: **التجهيز** (توفّر المخزون)، **الشراء** (تزويد الناقص عبر أوامر الشراء)، و**الربحية** (التكلفة `cost` مقابل السعر `price`).

**التصنيف الوظيفي:** كتالوج CRUD + نظام تتبّع كمّي متعدد المواقع.

---

# Business Purpose

| الغرض | التفصيل |
|------|---------|
| **معرفة ما يُباع** | كتالوج مركزي بأسعار وتكاليف المنتجات |
| **منع البيع بلا مخزون** | تتبّع الكمية المتاحة + تنبيهات النفاد + خيار السماح بالبيع عند النفاد |
| **دعم المتغيرات** | منتج واحد بعدة خيارات (لون/مقاس/عطر) بأسعار/مخزون مختلف |
| **حساب الربحية** | `cost` لكل منتج → تكلفة البضائع المباعة (COGS) |
| **تغذية التجهيز** | معرفة توفّر الصنف في المستودع المطلوب قبل الشحن |

---

# Functional Description

الوحدة تعرض كتالوج المنتجات مع مخزونها في **المستودع المحدد**، وتتيح:
- إضافة/تعديل/حذف منتج.
- إدارة متغيرات المنتج.
- تبديل المستودع لعرض مخزونه.
- إخفاء المنتجات غير المتوفرة.
- تبديل عرض جدول/شبكة.
- البحث.

---

# Complete Screen Breakdown

## القسم أ: شريط الأدوات ✅

| العنصر | الوظيفة | الثقة |
|--------|---------|------|
| **محدّد المستودع** | اختيار المستودع لعرض مخزونه (مع نجمة الافتراضي) | ✅ |
| **إضافة منتج** | فتح نموذج الإضافة | ✅ |
| **بحث** | نص (اسم/SKU) | ✅ |
| **إخفاء المنتجات غير المتوفرة** | مفتاح تصفية | ✅ |
| **تبديل العرض** | جدول ⇄ شبكة (Grid) | ✅ |

## القسم ب: جدول المنتجات ✅

| العمود | المحتوى | الثقة |
|-------|---------|------|
| **اسم المنتج** | `name` | ✅ |
| **رمز المنتج (SKU)** | `sku` (اختياري) | ✅ |
| **السعر** | `price` (EGP) | ✅ |
| **التكلفة** | `cost` (EGP) | ✅ |
| **المخزون** | `on_hand` في المستودع المحدد | ✅ |
| **مُباع** | الكمية المباعة | ✅ |
| **الحالة** | نفذ / متبقٍ N | ✅ |
| **الإجراءات** | عرض التفاصيل / إدارة المتغيرات / تعديل / حذف | ✅/🟡 |

**الترقيم:** 10 منتجات/صفحة في العرض، لكن يُحمّل حتى **10000 منتج داخليًا** في بعض الاستعلامات (`limit=10000`) ✅ — نقطة أداء.

## القسم ج: نموذج "إضافة منتج" (Dialog) ✅

| الحقل | التفصيل | إلزامي | الثقة |
|------|---------|--------|------|
| **الاسم** | اسم المنتج | نعم | ✅ |
| **رمز المنتج (SKU)** | معرّف اختياري | لا | ✅ |
| **السعر** | سعر البيع (EGP) | نعم | ✅ |
| **التكلفة** | تكلفة الشراء (EGP) | 🟡 | ✅ |
| **المخزون** | الكمية الابتدائية | 🟡 | ✅ |
| **حد المخزون المنخفض** | `low_stock_threshold` (عتبة التنبيه) | 🟡 | ✅ |
| **صورة** | تُضغط تلقائيًا قبل الرفع | لا | ✅ |
| **السماح بالبيع عند نفاد المخزون** | مفتاح (`allow_oversell`) | لا | ✅ |

## القسم د: نافذة "إدارة المتغيرات" (Variants) ✅

- **خصائص المتغير** (لون/مقاس/عطر) تُفعّل **قوائم متتالية (Cascading)** عند إنشاء الطلب.
- **جدول المتغيرات:** اسم المتغير*، السعر*، التكلفة، المخزون، رمز المنتج.
- **"إضافة متعددة سريعة"** (Bulk quick add بسعر/تكلفة/مخزون افتراضي) + حفظ الكل.
- كل متغير = صف منفصل في `products` بـ `parent_product_id` يشير للمنتج الأب.

---

# UI Components

| المكوّن | الاستخدام | الثقة |
|--------|-----------|------|
| **Table** | جدول المنتجات + جدول المتغيرات | ✅ |
| **Grid/Cards** | عرض الشبكة البديل | ✅ |
| **Dialog** | إضافة منتج + إدارة المتغيرات | ✅ |
| **Select** | محدّد المستودع | ✅ |
| **Switch/Toggle** | إخفاء غير المتوفر + السماح بالبيع عند النفاد | ✅ |
| **File Upload** | رفع الصورة (مع ضغط) | ✅ |
| **Badge** | حالة المخزون (نفذ/متبقٍ N) | ✅ |

---

# UX Analysis

**نقاط القوة ✅:**
- **المتغيرات بقوائم متتالية** — تجربة أنيقة لمنتجات متعددة الخيارات.
- **ضغط الصور تلقائيًا** — يخفف حجم النقل دون تدخّل المستخدم.
- **إخفاء غير المتوفر** — تركيز على القابل للبيع.
- **"إضافة متعددة سريعة" للمتغيرات** — تسريع إدخال منتج بخيارات كثيرة.
- **السماح بالبيع عند النفاد** — مرونة لنماذج البيع المسبق/الطلب حسب الطلب.

**نقاط الاحتكاك 🟡:**
- **تحميل حتى 10000 منتج** — بطء محتمل للكتالوجات الضخمة.
- **المخزون مرتبط بالمستودع المحدد فقط** — رؤية المخزون الكلي عبر المستودعات تحتاج تنقّلًا.

---

# Navigation

- **الوصول:** الشريط الجانبي → مجموعة "المخزون" → المنتجات.
- **الداخل:** قائمة → عرض تفاصيل منتج / إدارة متغيرات (Dialog).
- **التكامل:** يظهر في نموذج إنشاء الطلب (اختيار منتج) وفي أوامر الشراء (المنتج المطلوب).

---

# User Actions

| الإجراء | النوع | الثقة |
|--------|------|------|
| إضافة منتج | كتابة | ✅ |
| تعديل منتج | كتابة | 🟡 |
| حذف منتج | كتابة | 🟡 |
| إدارة متغيرات (إضافة/تعديل) | كتابة | ✅ |
| رفع صورة | كتابة | ✅ |
| تبديل السماح بالبيع عند النفاد | كتابة | ✅ |
| تبديل المستودع | قراءة | ✅ |
| بحث/إخفاء غير المتوفر | قراءة | ✅ |
| تبديل عرض جدول/شبكة | قراءة | ✅ |

---

# Buttons

| الزر | الموقع | الثقة |
|-----|--------|------|
| إضافة منتج | الشريط العلوي | ✅ |
| تبديل العرض | الشريط العلوي | ✅ |
| عرض التفاصيل / إدارة المتغيرات / تعديل / حذف | صف المنتج | ✅/🟡 |
| إضافة متعددة سريعة / حفظ الكل | نافذة المتغيرات | ✅ |
| حفظ / إلغاء | نموذج الإضافة | ✅ |

---

# Forms

## نموذج إضافة/تعديل منتج ✅
(موثّق في القسم ج: الاسم، SKU، السعر، التكلفة، المخزون، حد المخزون المنخفض، الصورة، السماح بالبيع عند النفاد).

## نموذج المتغيرات ✅
(موثّق في القسم د: اسم المتغير، السعر، التكلفة، المخزون، SKU + إضافة متعددة سريعة).

---

# Tables

| الجدول | الأعمدة | ترقيم |
|-------|---------|-------|
| المنتجات | الاسم · SKU · السعر · التكلفة · المخزون · مُباع · الحالة · الإجراءات | 10/صفحة (10000 داخليًا) |
| المتغيرات | اسم المتغير · السعر · التكلفة · المخزون · SKU | داخل Dialog |

---

# Cards

- **عرض الشبكة (Grid):** بطاقة لكل منتج (مع الصورة) — بديل الجدول، مناسب بصريًا للكتالوج.

---

# Charts

- **لا رسوم بيانية** في الوحدة (تحليل المخزون البصري في `/analytics` → تبويب المخزون: قيمة المخزون، تفصيل حسب المستودع). ✅

---

# Filters

- **المستودع** (محدّد) · **إخفاء غير المتوفر** (مفتاح) · **البحث** (نص). ✅
- لا فلاتر فئات/علامات تجارية مرصودة 🟡.

---

# Search

- **بحث نصّي:** الاسم / SKU 🟡.
- الآلية: فلتر PostgREST (`ilike`) 🟡.

---

# Sorting

- الترتيب الافتراضي: `created_at` 🟡.
- فرز الأعمدة اليدوي: غير مؤكد 🟡.

---

# Empty States

| الحالة | العرض المتوقّع | الثقة |
|-------|------------------|------|
| كتالوج فارغ | "لا منتجات — أضف أول منتج" + استيراد | 🟡 |
| كل المخزون نافد + إخفاء مفعّل | "لا منتجات متوفرة" | 🟡 |
| بحث بلا نتائج | "لا نتائج" | 🟡 |
| منتج بلا متغيرات | جدول متغيرات فارغ | 🟡 |

---

# Loading States

- تحميل الكتالوج: Skeleton/دوران 🟡.
- ضغط الصورة: مؤشر أثناء المعالجة المحلية 🟡.
- حفظ المتغيرات المتعددة: مؤشر أثناء الحفظ الجماعي 🟡.

---

# Error States

| الخطأ | السلوك | الثقة |
|------|--------|------|
| فشل رفع الصورة | رسالة + إبقاء باقي البيانات | 🟡 |
| SKU مكرر | رسالة تحقق (إن كان فريدًا) | 🟡 |
| فشل حفظ المتغيرات | رسالة + عدم فقدان الإدخال | 🟡 |

---

# Validation Rules

> التفصيل في `18_Business_Logic.md`.

| الحقل | القاعدة | الثقة |
|------|---------|------|
| الاسم | إلزامي | ✅ |
| السعر | إلزامي، ≥ 0 | 🟡 |
| التكلفة | ≥ 0 | ⚪ |
| المخزون | عدد صحيح ≥ 0 | ⚪ |
| حد المخزون المنخفض | ≥ 0 | ⚪ |
| اسم المتغير | إلزامي | ✅ |
| سعر المتغير | إلزامي | ✅ |
| SKU | مرجّح فريد ضمن الشركة | 🟡 |

---

# Permissions

> المصفوفة الكاملة في `15_Permissions.md`.

- **الوصول للمخزون:** الأدوار كاملة الصلاحية + **مدير المستودع** (المخزون هو مجاله الوحيد) ✅.
- **مدير المستودع:** بلا وصول للطلبات، لكن وصول كامل للمخزون 🟡.
- **الأدوار التشغيلية (كول سنتر/توصيل):** مرجّح قراءة فقط أو لا وصول 🟡.

---

# Business Rules

> التفصيل في `18_Business_Logic.md`.

1. **المخزون منسوب لمستودع:** لا "مخزون عام"؛ كل كمية في `inventory_stock` لزوج (منتج × مستودع). ✅
2. **`on_hand` مقابل `committed`:** `on_hand` = المتوفر فعليًا، `committed` = محجوز لطلبات قيد المعالجة. المتاح = `on_hand − committed` 🟡.
3. **المتغير منتج مستقل:** له `parent_product_id`، وسعره/تكلفته/مخزونه منفصلة. ✅
4. **تنبيه النفاد:** يُطلق عند `on_hand ≤ low_stock_threshold`. 🟡
5. **السماح بالبيع عند النفاد:** إن مُفعّل، يُسمح بطلب منتج نفد (بيع مسبق). ✅
6. **الربحية:** `cost` أساس حساب COGS في التحليلات. ✅
7. **زيادة المخزون:** عبر استلام أوامر الشراء (`po_receiving_logs`) أو التحويلات. ✅

---

# Workflow

```
إضافة منتج → (اختياري) إضافة متغيرات
   → تعيين مخزون ابتدائي في مستودع
   → بيع (طلب) → حجز committed → خصم on_hand عند الشحن/الإكمال 🟡
   → نفاد → تنبيه → أمر شراء → استلام → زيادة on_hand
   → (اختياري) تحويل بين المستودعات
```

---

# Internal Logic

- **جلب المنتجات:** RPC `get_inventory_products` ✅ (يجمع المنتج + مخزونه في المستودع + المباع).
- **المتغيرات:** `GET products?parent_product_id=not.is.null` ✅.
- **ضغط الصورة:** معالجة محلية في المتصفح قبل الرفع ✅.
- **حالة المخزون:** محسوبة من `on_hand` مقابل `low_stock_threshold`.

---

# State Changes

| الحدث | الأثر | الثقة |
|------|------|------|
| إنشاء طلب (قيد المعالجة) | `committed`+ | 🟡 |
| شحن/إكمال طلب | `on_hand`− (وتحرير committed) | 🟡 |
| استلام أمر شراء | `on_hand`+ في المستودع المستهدف | ✅ |
| تحويل مخزون | `on_hand`− من مصدر، +لهدف | ✅ |
| إلغاء طلب محجوز | تحرير `committed` | 🟡 |

---

# Possible Database Tables

| الجدول | الدور | الثقة |
|-------|------|------|
| `products` | الكتالوج + المتغيرات (self-reference) | ✅ |
| `inventory_stock` | المخزون لكل (منتج × مستودع) | ✅ |
| `inventories` | المستودعات | ✅ |
| `order_items` | مصدر "المباع" | ✅ |
| `po_receiving_logs` | مصدر زيادة المخزون | ✅ |
| `stock_transfers` | تحويلات المخزون | ✅ |

---

# Possible Database Fields

**`products` ✅/🟡:**
`id`, `company_id`, `name`, `sku`, `price`, `cost`, `low_stock_threshold`, **`parent_product_id` (FK→products, self)**, `variant_name`, `allow_oversell`🟡, `image_url`🟡, `created_at`.
**Index:** `(company_id)`, `(parent_product_id)`.

**`inventory_stock` ✅:**
`product_id`, `inventory_id`, `on_hand`, `committed`, `quantity`. **PK مرجّح:** `(inventory_id, product_id)`.

---

# Relationships

```
products (M) >── (1) companies         [company_id]
products (1) ──< products               [parent_product_id → المتغيرات]
products (1) ──< inventory_stock >── (M) inventories   [المخزون لكل مستودع]
products (1) ──< order_items            [المباع]
products (1) ──< po_items               [المشترى]
```

---

# Possible APIs

> التفصيل الكامل في `13_APIs.md`.

| الغرض | الطلب | الثقة |
|------|-------|------|
| جلب المنتجات + المخزون | `POST /rest/v1/rpc/get_inventory_products` | ✅ |
| جلب المتغيرات | `GET /rest/v1/products?...&parent_product_id=not.is.null` | ✅ |
| المخزون لكل مستودع | `GET /rest/v1/inventory_stock?select=*,on_hand,committed,products(...)` | ✅ |
| إضافة منتج | `POST /rest/v1/products` | 🟡 |
| تعديل منتج | `PATCH /rest/v1/products?id=eq.` | 🟡 |
| حذف منتج | `DELETE /rest/v1/products?id=eq.` | 🟡 |
| رفع صورة | تخزين Supabase Storage (مرجّح) | 🟡 |
| تحميل ضخم | `GET /rest/v1/products?...&limit=10000` | ✅ |

---

# Request Flow

```
فتح /inventory
  → تحديد المستودع (افتراضي: الرئيسي)
  → RPC get_inventory_products (منتجات + مخزون + مباع للمستودع المحدد)
تبديل المستودع
  → إعادة RPC للمستودع الجديد
إدارة متغيرات
  → GET products?parent_product_id=eq.<أب>
  → POST/PATCH products (لكل متغير) → حفظ الكل
```

---

# Response Flow

- `get_inventory_products` يعيد قائمة مجمّعة (منتج + on_hand + committed + مباع + حالة).
- المتغيرات تعود كصفوف products بـ parent محدد.

---

# Performance Considerations

**نقاط الضغط ✅:**
- **`limit=10000`** — تحميل الكتالوج الضخم دفعة واحدة (شاشة المستودعات/التحويلات).
- **ضغط الصور محليًا** — إيجابي (يقلل النقل).

**التوصيات (تُفصّل في `21`):** ترقيم خادمي حقيقي، تحميل كسول للصور، cache للكتالوج شبه الثابت.

---

# Security Considerations

- عزل `company_id` + RLS 🟡.
- **مدير المستودع** له وصول للمخزون فقط — يجب فرضه خادميًا 🟡.
- رفع الصور: تحقق من النوع/الحجم لمنع رفع ملفات خبيثة ⚪.

---

# Edge Cases

| الحالة | السلوك | الثقة |
|-------|--------|------|
| منتج بمخزون سالب | يجب منعه (إلا مع allow_oversell) | 🟡 |
| SKU مكرر | تحقق/منع | 🟡 |
| حذف منتج له طلبات سابقة | يجب أرشفة لا حذف صلب (حفظ `product_name` في order_items يخفف) | 🟡 |
| متغير بلا مخزون | يظهر "نفذ" | ✅ |
| منتج في عدة مستودعات | مخزون منفصل لكل مستودع | ✅ |
| صورة ضخمة | تُضغط تلقائيًا | ✅ |

---

# Failure Scenarios

- **فشل ضغط/رفع الصورة:** يجب حفظ المنتج بلا صورة لا فشل كامل 🟡.
- **تعارض تحديث مخزون متزامن:** بيعان متزامنان لآخر قطعة → يحتاج قفل/معاملة ذرية ⚪.
- **عدم تزامن on_hand مع committed:** أخطاء منطقية في الحجز/التحرير تُفسد المخزون ⚪.

---

# Logging Requirements

- تغييرات المخزون (استلام/تحويل/بيع) يجب تسجيلها لتتبّع الحركة ⚪.
- تعديل الأسعار/التكاليف يجب تسجيله (أثر على الربحية) ⚪.

---

# Audit Requirements

- **تدقيق حركة المخزون** — من غيّر الكمية، لماذا، متى (استلام/تحويل/تسوية) ⚪.
- **تدقيق تغيير التكلفة** — حرج لدقة COGS ⚪.

---

# Analytics Events

أحداث مرجّحة 🟡:
`product_created`, `product_edited`, `product_deleted`, `variant_added`, `variant_bulk_added`, `product_image_uploaded`, `inventory_warehouse_switched`, `inventory_searched`, `oversell_toggled`.

---

# Technical Notes

- **نمط المتغيرات (Self-referencing `parent_product_id`)** قرار تصميمي مهم: المتغير منتج كامل الحقوق (سعر/تكلفة/مخزون/SKU) لا مجرد سمة. يبسّط الطلبات لكنه يضخّم عدد الصفوف (يفسّر 564 منتجًا و`limit=10000`).
- **الفصل `on_hand`/`committed`** يعكس نضجًا في إدارة المخزون (حجز vs متاح).
- **ضغط الصور محليًا** يقلّل الاعتماد على معالجة خادمية.
- **تسمية `inventories` للمستودعات** — انتباه دائم في القاعدة والـ APIs.

---

# Reverse Engineering Notes

- بنية الكتالوج والنموذج والمتغيرات **مؤكدة ✅** (مشاهدة مباشرة).
- `get_inventory_products` و`inventory_stock` و`parent_product_id` **مؤكدة ✅** من الشبكة.
- `limit=10000` **مؤكد ✅**.
- آثار الحجز (`committed`) عند تغيير حالة الطلب **مرجّحة 🟡** — الحقل مرصود، آليته لا.
- تخزين الصور في Supabase Storage **مرجّح 🟡**.
- `allow_oversell` و`image_url` كأسماء أعمدة **مرجّحة 🟡** (الوظيفة مؤكدة).

---

# Confidence Level

| العنصر | الثقة |
|-------|------|
| بنية الكتالوج والجدول والأعمدة | ✅ مؤكد |
| نموذج الإضافة (كل الحقول) | ✅ مؤكد |
| المتغيرات (parent_product_id + إضافة متعددة) | ✅ مؤكد |
| المخزون لكل مستودع (on_hand/committed) | ✅ مؤكد |
| RPC get_inventory_products | ✅ مؤكد |
| limit=10000 | ✅ مؤكد |
| آلية الحجز/التحرير committed | 🟡 مرجّح |
| تخزين الصور | 🟡 مرجّح |
| أسماء أعمدة allow_oversell/image_url | 🟡 مرجّح |
| أحداث التتبّع | 🟡 مرجّح |

---

READY FOR NEXT FILE
