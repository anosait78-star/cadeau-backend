# 17 — UI / DESIGN SYSTEM
## توثيق نظام التصميم وواجهة المستخدم الكامل لنظام OrdersFlow

> **حالة الوثيقة:** مرجع رسمي معتمد (Authoritative Reference)
> **نطاق هذا الملف:** التوثيق الشامل لنظام التصميم — المكوّنات، الألوان، الطباعة، الأنماط، الأنماط التفاعلية، التخطيط، RTL، والثيم.
> **المكدّس:** Tailwind CSS + Radix UI / shadcn.
> **الطبيعة:** ملف **وصفي** (يصف نظام تصميم OrdersFlow كما هو). الاقتراحات في `21`.
> **تصنيف الثقة:** ✅ مؤكد · 🟡 مرجّح · ⚪ تخمين
> **هذا الملف مستقل بذاته** — يُقرأ دون الرجوع لأي ملف آخر.

---

# Overview

نظام تصميم OrdersFlow مبني على **shadcn/ui** (مكوّنات Radix UI مغلّفة بـ Tailwind CSS) — نمط تصميم حديث، نظيف، ومتّسق. هذا الاختيار يوفّر:
- **مكوّنات قابلة للوصول** (Radix يوفّر ARIA + سلوك لوحة المفاتيح).
- **تخصيص كامل** (Tailwind + متغيّرات CSS للثيم).
- **اتساق بصري** عبر النظام كله.

النظام **RTL-first** (عربي أولًا) مع دعم LTR (إنجليزي)، وثيم مزدوج (فاتح/داكن).

**التصنيف:** نظام تصميم قائم على المكوّنات (Component-based Design System) بنمط Utility-first CSS.

---

# Business Purpose (لنظام التصميم)

| الغرض | التفصيل |
|------|---------|
| **الاتساق** | مظهر موحّد يقلّل الحمل الإدراكي |
| **سرعة التطوير** | مكوّنات جاهزة قابلة لإعادة الاستخدام |
| **إمكانية الوصول** | أساس Radix يوفّر امتثالًا أوليًا |
| **المرونة** | Tailwind + متغيّرات للثيم/RTL |
| **الهوية البصرية** | مظهر احترافي يبني الثقة (لنظام مالي) |

---

# الأساس التقني (Technical Foundation) ✅

| الطبقة | التقنية | الدليل | الثقة |
|-------|---------|--------|------|
| **CSS Framework** | Tailwind CSS | أنماط الفئات المرصودة (`inline-flex`, `rounded-md`) | ✅ |
| **مكتبة المكوّنات** | Radix UI (عبر shadcn) | خصائص `data-radix-*` | ✅ |
| **الأيقونات** | Lucide (مرجّح) | ظهور `file-down` | 🟡 |
| **الثيم** | متغيّرات CSS (فاتح/داكن) | مفتاح `theme` في localStorage | ✅ |
| **الاتجاه** | RTL/LTR | تخطيط معكوس عند العربية | ✅ |
| **الخطوط** | خط يدعم العربية | عرض عربي سليم | ✅ |

---

# مكتبة المكوّنات (Component Library) ✅

## المكوّنات الأساسية المرصودة

| المكوّن | Radix Primitive | الاستخدام في النظام | الثقة |
|--------|-----------------|---------------------|------|
| **Dialog** | `@radix-ui/dialog` | نماذج الإنشاء (طلب/منتج/مصروف) | ✅ |
| **Sheet** | `@radix-ui/dialog` (variant) | لوحة تفاصيل الطلب الجانبية | ✅ |
| **Tabs** | `@radix-ui/tabs` | تبويبات الحالة، التحليلات، الإعدادات، لوحة التفاصيل | ✅ |
| **Combobox** | `@radix-ui/popover` + command | تغيير الحالة inline، الإسناد، المتغيرات | ✅ |
| **Checkbox** | `@radix-ui/checkbox` | التحديد الجماعي | ✅ |
| **Select** | `@radix-ui/select` | القوائم المنسدلة (فئة/مستودع/فلاتر) | ✅ |
| **Dropdown Menu** | `@radix-ui/dropdown-menu` | قائمة الشركة، الإشعارات، إجراءات الصف | ✅ |
| **Switch/Toggle** | `@radix-ui/switch` | إخفاء غير المتوفر، تفعيل الإشعارات، السماح بالبيع | ✅ |
| **Popover** | `@radix-ui/popover` | Date Range Picker، القوائم | ✅ |
| **Tablist** | `@radix-ui/tabs` | شرائط التبويب | ✅ |

## مكوّنات مركّبة (Composite)

| المكوّن | مبني من | الاستخدام | الثقة |
|--------|---------|-----------|------|
| **Card** | div + Tailwind | بطاقات KPI، الحاويات | ✅ |
| **Table** | table + Tailwind | جداول الطلبات/العملاء/المنتجات | ✅ |
| **Badge** | span + Tailwind | الحالات، التصنيفات، حالة الدفع | ✅ |
| **Button** | button + Tailwind (variants) | كل الأزرار | ✅ |
| **Stat/Metric** | Card + typography | مؤشرات KPI | ✅ |
| **Date Range Picker** | Popover + Calendar | الفلاتر الزمنية | ✅ |
| **File Upload** | input + معالجة | رفع الصور/الاستيراد | ✅ |

---

# نظام الألوان (Color System) ✅/🟡

## البنية (متغيّرات CSS بنمط shadcn) 🟡
النظام يستخدم متغيّرات CSS دلالية (Semantic Tokens) بنمط shadcn:

| الرمز الدلالي | الاستخدام | الثقة |
|--------------|-----------|------|
| `--background` | خلفية الصفحة | 🟡 |
| `--foreground` | النص الأساسي | 🟡 |
| `--primary` | اللون الأساسي (أزرار/روابط) | 🟡 |
| `--muted` | نص/خلفيات ثانوية | 🟡 |
| `--border` | الحدود | 🟡 |
| `--card` | خلفية البطاقات | 🟡 |
| `--destructive` | الحذف/الخطر | 🟡 |

## الألوان الدلالية للحالات ✅
- كل حالة طلب لها لون دلالي (Status Colors) — أخضر للمكتمل، أحمر للملغي، إلخ (المخطط الدقيق غير موثّق لونيًا) 🟡.
- **التصنيفات ملوّنة مخصّصة** — `order_labels.color` يخزّن لون كل تصنيف (مستعجل/مكرر) ✅.

## الثيم المزدوج ✅
- **فاتح/داكن** مدعوم عبر متغيّرات CSS تتبدّل حسب `theme` في localStorage.
- التبديل من الشريط العلوي (مبدّل الثيم).

---

# الطباعة (Typography) ✅/🟡

| الجانب | التفصيل | الثقة |
|-------|---------|------|
| **الخط** | يدعم العربية (عرض سليم) — الاسم الدقيق غير مرصود | ✅/⚪ |
| **الهرمية** | أحجام متدرّجة للعناوين/الوصف/النص | ✅ |
| **الأوزان** | عادي/متوسط/عريض (نمط Tailwind) | 🟡 |
| **RTL** | محاذاة يمين للنص العربي | ✅ |
| **الأرقام** | أرقام عربية غربية (1،2،3) مع فواصل | 🟡 |

---

# الأنماط والمكوّنات البصرية (Visual Patterns) ✅

## البطاقات (Cards)
- حواف مستديرة (`rounded-md`/`rounded-lg`).
- ظلال خفيفة (`shadow-sm`).
- حشو داخلي متسق (`p-4`/`p-6`) 🟡.
- عنوان + محتوى + (اختياري) تذييل.

## الأزرار (Buttons)
| النوع | النمط | الاستخدام |
|------|------|-----------|
| **أساسي (Primary)** | خلفية `--primary` | الإجراء الرئيسي (إنشاء/حفظ) |
| **ثانوي (Secondary)** | حدود/خلفية خفيفة | إلغاء/إجراءات ثانوية |
| **أيقوني (Icon)** | أيقونة فقط | إجراءات مدمجة (تعديل/حذف) |
| **مدمّر (Destructive)** | `--destructive` | حذف |

نمط عام: `inline-flex … rounded-md` ✅.

## الجداول (Tables)
- صفوف بحدود سفلية (`border-b`).
- تأثير Hover على الصف.
- عمود الإجراءات محاذى للنهاية (RTL → اليسار).
- Checkbox تحديد في الرأس والصفوف.
- رأس بخلفية/نص مميّز.

## الشارات (Badges)
- حواف مستديرة كاملة (`rounded-full`) أو `rounded-md` 🟡.
- ألوان دلالية (حالة/تصنيف/دفع).
- حجم صغير مدمج.

## الحوارات واللوحات (Dialogs & Sheets)
- **Dialog:** منبثق مركزي مع overlay معتم (النماذج).
- **Sheet:** لوحة منزلقة جانبية (تفاصيل الطلب) — من اليمين في RTL 🟡.
- زر إغلاق + رأس + محتوى + تذييل أزرار.

---

# نظام التخطيط (Layout System) ✅

## البنية العامة
```
┌─────────────────────────────────────────────┐
│  الشريط العلوي (إشعارات · ثيم · شركة)          │
├──────────┬──────────────────────────────────┤
│ الشريط    │                                  │
│ الجانبي   │        منطقة المحتوى               │
│ (وحدات    │      (الشاشة النشطة)               │
│ + مجموعات │                                  │
│ قابلة     │                                  │
│ للطي)     │                                  │
└──────────┴──────────────────────────────────┘
```

## الشريط الجانبي (Sidebar) ✅
- قائمة الوحدات مع أيقونات.
- **مجموعات قابلة للطي:** المخزون (منتجات+مستودعات)، الماليات (موردون+أوامر+إقفال).
- تمييز الوحدة النشطة.
- في RTL: على اليمين.

## الشريط العلوي (Topbar) ✅
- جرس الإشعارات.
- مبدّل الثيم.
- قائمة الشركة (تبديل/إنشاء/انضمام).

## Grid/Flex ✅
- بطاقات KPI في شبكة (Grid).
- أدوات الشريط في Flex.
- تخطيط متجاوب (مرجّح breakpoints Tailwind) 🟡.

---

# دعم RTL (Right-to-Left) ✅

| الجانب | التطبيق | الثقة |
|-------|---------|------|
| **اتجاه التخطيط** | معكوس كليًا عند العربية | ✅ |
| **الشريط الجانبي** | على اليمين | ✅ |
| **أعمدة الإجراءات** | على اليسار (نهاية RTL) | ✅ |
| **محاذاة النص** | يمين | ✅ |
| **الأيقونات الاتجاهية** | معكوسة (أسهم) | 🟡 |
| **Sheet** | ينزلق من اليمين | 🟡 |

> **قوة (✅):** RTL أصيل لا مجرد `dir="rtl"` سطحي — التخطيط مصمّم للعربية.

---

# الأنماط التفاعلية (Interaction Patterns) ✅

| النمط | الوصف | الثقة |
|------|-------|------|
| **Inline Editing** | تغيير الحالة/الإسناد داخل صف الجدول (Combobox) | ✅ |
| **Bulk Selection** | Checkbox متعدد + إجراءات جماعية | ✅/🟡 |
| **Side Sheet** | تفاصيل الطلب في لوحة جانبية (لا مغادرة الصفحة) | ✅ |
| **Modal Forms** | الإنشاء في Dialog منبثق | ✅ |
| **View Toggle** | جدول ⇄ بطاقات | ✅ |
| **Cascading Selects** | قوائم متتالية للمتغيرات | ✅ |
| **Smart Paste** | لصق نص → استخراج تلقائي | ✅ |
| **Collapsible Groups** | مجموعات الشريط الجانبي | ✅ |
| **Persistent State** | حفظ الفلاتر/العرض | ✅ |

---

# حالات المكوّنات (Component States) 🟡

| الحالة | التطبيق المتوقّع | الثقة |
|-------|------------------|------|
| **Default** | الحالة العادية | ✅ |
| **Hover** | تظليل الصفوف/الأزرار | ✅ |
| **Active/Selected** | تمييز الوحدة/التبويب النشط | ✅ |
| **Disabled** | تعطيل حسب الصلاحية/الشرط | 🟡 |
| **Loading** | Skeleton/دوران (مرجّح) | 🟡 |
| **Error** | حدود/نص أحمر | 🟡 |
| **Empty** | حالة فارغة (غير مؤكدة الجودة) | 🟡 |
| **Focus** | حلقة تركيز (Radix يوفّرها) | 🟡 |

---

# الأيقونية (Iconography) 🟡

- **المكتبة:** Lucide (مرجّح — ظهر `file-down`) 🟡.
- **الاستخدام:** أيقونات SVG في الأزرار، البطاقات، الشريط الجانبي، الإجراءات.
- **الحجم:** متسق (16/20px نمطيًا) ⚪.
- **الاتجاه:** الأيقونات الاتجاهية معكوسة في RTL 🟡.

---

# التجاوب (Responsive Design) 🟡

- **مرجّح breakpoints Tailwind** (sm/md/lg/xl) 🟡.
- **عرض البطاقات** بديل الجدول للشاشات الضيقة ✅.
- **الشريط الجانبي** مرجّح ينهار لقائمة/درج على الموبايل ⚪.
- **PWA مرجّح** (Web Push + ضغط صور محلي) 🟡.

---

# الاتساق البصري (Visual Consistency) ✅/🟡

| الجانب | الحالة | الثقة |
|-------|--------|------|
| نمط الأزرار/البطاقات/الجداول | متسق (نظام موحّد) | ✅ |
| المسافات (Spacing) | متسقة (نظام Tailwind) | 🟡 |
| الحواف/الظلال | متسقة | ✅ |
| الألوان الدلالية | متسقة | ✅ |
| **تنسيق العملة** | **غير متسق** ($ في العملاء) | ✅ |

---

# Accessibility (على مستوى UI) 🟡

- **Radix يوفّر أساسًا قويًا:** ARIA roles، إدارة التركيز، تنقّل لوحة المفاتيح، إعلانات قارئ الشاشة للحوارات/القوائم 🟡.
- **الثيم المزدوج** يدعم تفضيلات التباين ✅.
- **RTL** مدعوم أصيلًا ✅.
- **غير مؤكد:** تباين الألوان (WCAG)، أحجام أهداف اللمس، اختبار قارئ الشاشة الفعلي ⚪.

---

# Technical Notes

- **shadcn/ui ليس مكتبة تُثبّت بل كود يُنسخ** — المكوّنات جزء من قاعدة الكود، قابلة للتخصيص الكامل. مهم لإعادة البناء (ملكية الكود).
- **متغيّرات CSS للثيم** تسمح بتبديل فاتح/داكن وتخصيص العلامة التجارية بسهولة.
- **RTL الأصيل** أصل مهم — يجب الحفاظ عليه في إعادة البناء (لا `dir` سطحي).
- **عدم اتساق العملة** خلل في طبقة العرض (تنسيق) لا التصميم — سهل الإصلاح لكنه يضر الثقة.
- **حالات المكوّنات (Loading/Empty/Error)** غير مؤكدة التغطية — منطقة تحتاج تدقيقًا في إعادة البناء.

---

# Reverse Engineering Notes

- **Tailwind + Radix/shadcn مؤكد ✅** من `data-radix-*` وأنماط الفئات وأسماء الحزم (`ui-*.js`).
- **المكوّنات المستخدمة مؤكدة ✅** بالمشاهدة (Dialog/Sheet/Tabs/Combobox...).
- **نظام الألوان الدلالي مرجّح 🟡** — نمط shadcn القياسي، القيم الدقيقة غير مرصودة.
- **Lucide مرجّح 🟡** (أيقونة واحدة مرصودة).
- **الطباعة/المسافات/التجاوب مرجّحة 🟡/⚪** — نمط Tailwind، التفاصيل غير مقيسة.
- **إمكانية الوصول مستنتَجة من Radix** لا مختبرة ⚪.

---

# Confidence Level

| العنصر | الثقة |
|-------|------|
| Tailwind + Radix/shadcn كأساس | ✅ مؤكد |
| المكوّنات المستخدمة | ✅ مؤكد |
| الثيم المزدوج (فاتح/داكن) | ✅ مؤكد |
| RTL الأصيل | ✅ مؤكد |
| الأنماط التفاعلية (inline/sheet/cascading) | ✅ مؤكد |
| التصنيفات الملوّنة (order_labels.color) | ✅ مؤكد |
| نظام الألوان الدلالي (القيم) | 🟡 مرجّح |
| Lucide كمكتبة أيقونات | 🟡 مرجّح |
| الطباعة/المسافات/التجاوب (التفاصيل) | 🟡/⚪ |
| إمكانية الوصول | 🟡/⚪ |

---

READY FOR NEXT FILE
