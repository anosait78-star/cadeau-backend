# 01 — SYSTEM OVERVIEW
## نظرة عامة هندسية شاملة على نظام OrdersFlow

> **حالة الوثيقة:** مرجع رسمي معتمد (Authoritative Reference)
> **نطاق هذا الملف:** الرؤية الكاملة للنظام، هويته، جمهوره، وحداته، حدوده، ونموذج أعماله — كأساس مفاهيمي لكل الملفات التالية.
> **النظام:** OrdersFlow — `https://app.ordersflow.site`
> **الشركة المرجعية:** Cadeau Egypt (متجر هدايا/ورد/عطور — مصر)
> **تصنيف الثقة:** ✅ مؤكد · 🟡 مرجّح · ⚪ تخمين
> **هذا الملف مستقل بذاته** — يُقرأ دون الرجوع لأي ملف آخر.

---

# Overview

OrdersFlow هو **نظام SaaS متعدد المستأجرين (Multi-Tenant)** لإدارة الطلبات والعملاء، مصمّم خصيصًا لتجار التجزئة الذين يبيعون عبر قنوات السوشيال ميديا ويعتمدون على **الدفع عند الاستلام (COD)**. النظام يوحّد في منتج واحد ما كان التاجر يديره عبر أدوات متفرقة (واتساب + إكسيل + ورق + مكالمات).

من منظور التصنيف الوظيفي، النظام هجين يجمع ثلاث فئات من الأنظمة:

| الفئة | النصيب في النظام | الأدلة | الثقة |
|------|------------------|--------|------|
| **OMS** (Order Management System) | النواة — الوزن الأكبر | 12 حالة طلب، دورة حياة كاملة، شحن، تحصيل | ✅ |
| **CRM** (Customer Relationship) | ثانوي قوي | قاعدة عملاء (1000+)، ملف عميل، سجل طلبات، KPIs للعميل | ✅ |
| **ERP خفيف** (Light ERP) | مكمّل | مخزون متعدد المستودعات، موردون، أوامر شراء، مصروفات، إقفال شهري، مركز نقدي | ✅ |

**الاستنتاج (🟡):** OrdersFlow ليس CRM تقليديًا ولا ERP كاملًا، بل **"COD Commerce Operations Platform"** — منصة تشغيل عمليات تجارية موجّهة لسوق الدفع عند الاستلام. هذا التموضع هو مفتاح فهم كل قرارات التصميم فيه (خاصة `collected_amount` وتكامل شركات الشحن).

---

# Business Purpose

## المشكلة الجوهرية التي يحلّها النظام (✅)

تاجر السوشيال كومرس المصري يواجه **تشتت العمليات**:

1. الطلبات تصل عبر رسائل انستجرام/واتساب/فيسبوك بصيغة نصية غير منظّمة.
2. التأكيد يتم هاتفيًا (كول سنتر) — كثير من الطلبات "تيست" أو مكررة أو العميل لا يرد.
3. التجهيز يتطلب معرفة المخزون الفعلي عبر مواقع متعددة.
4. الشحن يتم عبر شركات توصيل متعددة، كل منها ببوليصة ونظام تتبع مختلف.
5. التحصيل COD يعني أن المبلغ المستلم فعليًا **أقل** من قيمة الطلب (رسوم الشحن تُخصم من التاجر).
6. حساب الربحية الحقيقية شبه مستحيل يدويًا (إيراد − تكلفة بضاعة − مصروفات − رسوم شحن − مرتجعات).

## القيمة التي يقدّمها OrdersFlow (✅)

| المشكلة | الحل في OrdersFlow |
|--------|---------------------|
| طلبات نصية غير منظّمة | **اللصق الذكي (Smart Paste)** يستخرج البيانات تلقائيًا |
| فوضى التأكيد | حالات طلب صريحة (جديد/جاري التأكيد) + قوالب واتساب + أسباب إلغاء |
| جهل المخزون | مخزون لحظي لكل مستودع (`on_hand`/`committed`) + تنبيهات نفاد |
| تعدد شركات الشحن | تكامل مباشر (Bosta وغيرها) + شحن بالجملة + طباعة بوالص |
| غموض التحصيل | حقل `collected_amount` = المستلم فعليًا بعد رسوم الشحن |
| غياب الربحية | تحليلات + P&L شهري + مركز نقدي + إقفال شهري |

## نموذج التسعير (✅)

اشتراك SaaS شهري بخطط. شوهدت خطة **"الاحترافي"** مع تاريخ انتهاء، أيام متبقية، وزر "عرض الخطط". التفعيل يتم عبر جدول `company_features` (Feature Flags لكل شركة) — ما يعني أن الخطط تتحكم في **الميزات المتاحة** لكل مستأجر، لا في حدود الاستخدام فقط (🟡).

---

# Functional Description

النظام يغطّي **دورة العمليات التجارية الكاملة** من استقبال الطلب حتى إقفال الحسابات:

```
[استقبال الطلب] → [التأكيد] → [التجهيز] → [الشحن] → [التسليم] → [التحصيل] → [التحليل والإقفال]
       ↑                                                                              ↓
   العملاء / المخزون / الموردون ←──────────── تغذية راجعة (ربحية، مخزون، مصروفات) ──────┘
```

كل مرحلة مدعومة بوحدة أو أكثر، وكلها معزولة على مستوى الشركة (`company_id`).

---

# الوحدات الأساسية (Core Modules) — 14 وحدة

> جرد كامل؛ التفاصيل عنصرًا عنصرًا في ملفات الشاشات (`03`–`10`).

| # | الوحدة | المسار | الغرض | الثقة |
|---|--------|--------|-------|------|
| 1 | **Dashboard** | `/` | نظرة يومية سريعة على الأداء (KPIs، حالات، تنبيهات) | ✅ |
| 2 | **Orders** | `/orders` | القلب النابض — إدارة كل الطلبات وتتبعها | ✅ |
| 3 | **Bulk Shipment** | `/orders/bulk-shipment` | إنشاء شحنات لعدة طلبات دفعة واحدة | ✅ |
| 4 | **Customers** | `/customers` | قاعدة العملاء والملفات التفصيلية | ✅ |
| 5 | **Team & Roles** | `/team` | إدارة الأعضاء والأدوار ورمز الدعوة | ✅ |
| 6 | **Inventory (Products)** | `/inventory` | كتالوج المنتجات والمخزون والمتغيرات | ✅ |
| 7 | **Warehouses** | `/warehouses` | المستودعات وتحويلات المخزون | ✅ |
| 8 | **Expenses** | `/expenses` | تتبع المصروفات المصنّفة | ✅ |
| 9 | **Analytics** | `/analytics` | التحليلات والتقارير (5 تبويبات) | ✅ |
| 10 | **Shipping** | `/settings` + `/shipping/settlement` | شركات التوصيل، المناطق، التسوية | ✅ (التسوية معطوبة) |
| 11 | **Finances** | `/finances/*` | الموردون + أوامر الشراء + الإقفال + المركز النقدي | ✅ |
| 12 | **Settings** | `/settings` | 7 تبويبات إعدادات | ✅ |
| 13 | **Notifications** | (شريط علوي) | إشعارات Web Push | ✅ |
| 14 | **Multi-Company** | (قائمة الشركة) | إنشاء/انضمام/تبديل بين شركات | ✅ |

**ملاحظة تجميع (🟡):** في الشريط الجانبي، الوحدات مُجمّعة في مجموعات قابلة للطي: "المخزون" (المنتجات + المستودعات) و"الماليات" (الموردون + أوامر الشراء + الإقفال + المصروفات). هذا التجميع تصميمي وليس بنيويًا.

---

# نطاق النظام (System Scope)

## ما هو داخل النطاق (In Scope) ✅

- إدارة دورة حياة الطلب كاملة (12 حالة).
- إدارة العملاء (CRM أساسي).
- كتالوج منتجات + متغيرات + مخزون متعدد المستودعات.
- سلسلة التوريد المصغّرة (موردون + أوامر شراء + استلام).
- المحاسبة النقدية (مصروفات + مركز نقدي + إقفال شهري + P&L).
- الشحن (تكامل شركات، مناطق، بوالص، شحن بالجملة).
- التحليلات والتقارير.
- إدارة الفريق والصلاحيات (9 أدوار).
- تعدد المستأجرين والشركات.
- الإشعارات (Web Push) والاستيراد (Excel/CSV).
- تكامل واتساب (قوالب + بادئة دولة).

## ما هو خارج النطاق (Out of Scope) — بناءً على ما لم يُرصد ❌

| البند | الحالة | الثقة |
|------|--------|------|
| **فواتير رسمية (Invoices)** | غير موجودة (فقط بوالص شحن + نسخ تفاصيل) | ✅ |
| **الضرائب / VAT** | لا حقل ضريبة مرصود | ✅ |
| **استرداد مالي (Refunds) كوحدة** | فقط حالة "مرتجع"، لا محاسبة استرداد مستقلة | 🟡 |
| **بوابة عميل نهائي (Customer Portal)** | لا تتبع للعميل النهائي ظاهر | 🟡 |
| **باركود / QR للمخزون** | غير مرصود | 🟡 |
| **تكامل متجر إلكتروني مباشر (POS/Store sync)** | فقط استيراد يدوي (رغم وجود طلبات بأرقام `WC-` توحي بـ WooCommerce مستورد) | 🟡 |
| **مبدّل عملة كامل** | العملة ثابتة (EGP)، فقط بادئة الدولة قابلة للضبط | ✅ |
| **صلاحيات دقيقة (Granular Permissions)** | الصلاحيات مرتبطة بحالات الطلبات فقط | ✅ |

> هذه الحدود مهمة جدًا لأي منتج منافس: النواقص أعلاه تمثّل **فرص تمايز** موثّقة تفصيلًا في `19_Missing_Features.md` و`21_Improvement_Ideas.md`.

---

# الجمhور المستهدف (Target Audience / Personas)

## الجمهور التجاري للنظام الأصلي (🟡 مرجّح من السياق)

**العميل (المشتري للنظام):** تاجر تجزئة صغير/متوسط في مصر أو الشرق الأوسط:
- يبيع منتجات استهلاكية (هدايا، عطور، ملابس، إكسسوارات) عبر انستجرام/فيسبوك/واتساب.
- حجم أعمال: مئات إلى آلاف الطلبات شهريًا.
- فريق: 5–20 شخصًا بأدوار متنوعة (كول سنتر، تجهيز، شحن، محاسبة).
- يعتمد COD كنموذج دفع رئيسي.

## المستخدمون داخل النظام (User Personas) ✅

مستنتجة من نظام الأدوار الـ9 (التفاصيل في `15_Permissions.md`):

| Persona | الدور في النظام | احتياجه الأساسي |
|---------|-----------------|-----------------|
| **صاحب العمل / المدير** | مسؤول / مدير | رؤية شاملة، تحليلات، ماليات، إدارة الفريق |
| **موظف الكول سنتر** | كول سنتر | تأكيد الطلبات هاتفيًا، تحديث الحالة |
| **موظف التجهيز** | تجهيز (Fulfillment) | تغليف الطلبات الجاهزة، إدارة النواقص |
| **موظف الشحن** | شحن | إنشاء الشحنات، تتبع التسليم |
| **مندوب التوصيل** | توصيل (Delivery) | رؤية محدودة للطلبات الجاهزة/المشحونة |
| **مدير المستودع** | مستودع | إدارة المخزون فقط (لا وصول للطلبات) |
| **المحاسب** | محاسب (🟡) | الماليات (لا حالات طلبات) |

---

# اللغة والتوطين (Localization)

| البند | التفصيل | الثقة |
|------|---------|------|
| **اللغات** | عربي (RTL) + إنجليزي — قابلة للتبديل من الإعدادات | ✅ |
| **الاتجاه** | RTL أصيل عند العربية (تخطيط معكوس، أعمدة الإجراءات لليسار) | ✅ |
| **العملة** | الجنيه المصري (ج.م. / EGP) — ثابتة | ✅ |
| **بادئة الدولة** | قابلة للضبط (20 لمصر) — تُستخدم في واتساب والهواتف | ✅ |
| **حالة اللغة المحفوظة** | `app-language` في localStorage | ✅ |
| **خلل موطنة مرصود** | صفحة العملاء تعرض `$0.00` بدل ج.م. — عدم اتساق عملة | ✅ |

---

# نموذج تعدد المستأجرين (Multi-Tenancy Model)

> ملخص هنا للاستقلالية؛ التفصيل المعماري في `02_Architecture.md`.

- **الكيان الجذر:** `companies` (الشركة = المستأجر).
- **العزل:** كل جدول يحمل `company_id`، وكل استعلام يُفلتر به: `?company_id=eq.<uuid>` ✅.
- **حالة المستخدم:** `currentCompanyId` مخزّن في localStorage ✅.
- **تعدد الشركات للمستخدم الواحد:** المستخدم يمكنه إنشاء/الانضمام/التبديل بين عدة شركات ✅.
- **العضوية:** عبر جدول `company_members` (user_id + company_id + role + status) ✅.
- **الأمان المرجّح:** Row Level Security (RLS) في Postgres حسب عضوية `company_members` 🟡.

**الاستنتاج المعماري (🟡):** هذا نموذج **Shared Database / Shared Schema** — كل المستأجرين في نفس الجداول، معزولون بـ `company_id` + RLS. هو الأبسط تشغيليًا والأكثر شيوعًا في SaaS الصغير/المتوسط.

---

# القيم والأرقام المرجعية (Reference Data — Cadeau Egypt) ✅

أرقام حقيقية من حساب الاختبار، مفيدة كمرجع لأحجام البيانات وتقدير الأداء:

| المؤشر | القيمة | الوحدة المعنية |
|--------|-------|-----------------|
| إجمالي الطلبات | 203 | Orders |
| إجمالي العملاء | 1,053 | Customers |
| إجمالي المنتجات | 564 | Inventory |
| أعضاء الفريق | 9 | Team |
| المستودعات النشطة | 3 (561 + 3 + 0 منتج) | Warehouses |
| الموردون | 4 | Finances |
| صافي الدخل | ~57,467 ج.م. | Analytics |
| تنبيهات المخزون المنخفض | 548 عنصرًا | Dashboard |
| رمز دعوة الشركة | `a8f05377` | Team |

> **دلالة الأداء:** 1053 عميلًا و564 منتجًا و548 تنبيه مخزون تشير إلى أن النظام يجب أن يتعامل بكفاءة مع قوائم بالمئات لكل مستأجر — وهو ما ينعكس في حدود الاستعلام (`limit=1000` للطلبات، `limit=10000` للمنتجات). تفاصيل في `13_APIs.md`.

---

# Business Rules (قواعد الأعمال على مستوى النظام)

> قواعد عامة تحكم النظام كله؛ القواعد التفصيلية لكل وحدة في `18_Business_Logic.md`.

1. **عزل المستأجر مطلق:** لا يمكن لأي عملية أن تعبر حدود `company_id`. ✅
2. **الطلب هو مركز الثقل:** كل الوحدات الأخرى تخدم دورة حياة الطلب أو تتغذى منها. ✅
3. **المبلغ المحصّل ≠ الإجمالي:** في COD، الربحية تُحسب على `collected_amount` (بعد رسوم الشحن) لا على `total`. ✅
4. **الصلاحيات مبنية على الحالات:** ما يراه/يغيّره المستخدم يُحدَّد بحالات الطلب المسموحة لدوره. ✅
5. **الإقفال الشهري متسلسل:** يجب إقفال الأشهر بالترتيب الزمني، ولا يُقفل شهر وبه طلبات مفتوحة أو أوامر شراء غير مستلمة. ✅
6. **المخزون مرتبط بالمستودع:** لا يوجد "مخزون عام"؛ كل كمية منسوبة لمستودع محدد. ✅
7. **المتغيرات أبناء للمنتج:** المتغير صف منفصل بـ `parent_product_id`، وله مخزونه وسعره وتكلفته. ✅

---

# Workflow (سير العمل عالي المستوى)

> الدورات التفصيلية في `14_Workflows.md`. هنا الصورة الكبرى فقط.

**الدورة الرئيسية (دورة الطلب):**
```
استقبال (يدوي/لصق ذكي/استيراد/متجر) → [جديد]
   → تأكيد العميل → [جاري التأكيد]
   → معالجة/تجهيز (حجز مخزون) → [قيد المعالجة → ناقص؟ → جاهز]
   → إنشاء شحنة → [تم الشحن]
   → تسليم COD → [تم التسليم]
   → تحصيل (collected_amount) → [مكتمل]
```
**مسارات فرعية:** مؤجل · ملغي (بسبب) · مرتجع · مستبدل.

**الدورات المساندة:** دورة الشراء والمخزون · الدورة المالية والإقفال · دورة إعداد الشركة (Onboarding) · دورة الإسناد وقياس الأداء.

---

# Security Considerations (على مستوى النظام)

> التفاصيل في `02_Architecture.md` و`13_APIs.md`.

- **المصادقة:** Supabase Auth (GoTrue) بتوكن JWT. ✅
- **العزل:** `company_id` + RLS مرجّح على مستوى الصف. 🟡
- **الوصول للبيانات:** عبر PostgREST مباشرة من المتصفح بتوكن المستخدم — ما يعني أن **RLS هو خط الدفاع الأساسي** (وليس طبقة API وسيطة). ✅ (استنتاج معماري مهم).
- **العمليات الحساسة:** حذف الحساب، تغيير كلمة المرور — عبر GoTrue. ✅
- **نقطة انتباه (⚪):** الاعتماد على PostgREST المباشر يعني أن أي خطأ في سياسات RLS يكشف بيانات مستأجرين آخرين — نقطة حرجة لأي إعادة بناء.

---

# Edge Cases & Known Issues (على مستوى النظام)

| الحالة | الوصف | الثقة |
|-------|-------|------|
| **شاشة تسوية الشحن معطوبة** | `/shipping/settlement` تتعطل بـ React error #310 (شاشة بيضاء) | ✅ |
| **كسر Deep-linking** | إعادة تحميل المسارات المتداخلة (`/finances/*`, `/customers/{id}`) تعيد للوحة التحكم | ✅ |
| **عدم اتساق العملة** | صفحة العملاء بالدولار بينما النظام بالجنيه | ✅ |
| **نسب لوحة التحكم ثابتة** | نسب "من الأسبوع الماضي" تبدو غير محسوبة (Placeholder) | 🟡 |
| **قوائم بلا ترقيم** | تنبيهات المخزون (548 عنصرًا) بلا ترقيم/طيّ | ✅ |
| **غموض دور المحاسب** | غير مُعرَّف في دليل حالات الطلبات | ✅ |

---

# Technical Notes

- **نمط التطبيق:** SPA (Single Page Application) بـ React + Vite، تقسيم حزم لكل صفحة. ✅
- **لا خادم تطبيقات وسيط تقليدي:** المنطق موزّع بين المتصفح (React) وقاعدة البيانات (PostgREST + RPC + RLS) + Edge Functions للتكاملات. ✅
- **دليل إعادة البناء (`ordersV2`):** وجود مفاتيح `ordersV2FilterState`/`ordersV2ViewMode` في localStorage يدل 🟡 على أن وحدة الطلبات مرّت بإعادة بناء — إشارة لأهميتها وتعقيدها.

---

# Reverse Engineering Notes

- كل الأرقام المرجعية من شركة Cadeau Egypt الحقيقية عبر حساب اختبار.
- تصنيف النظام كـ "COD Commerce Operations Platform" هو **استنتاج تحليلي (🟡)** مبني على مركزية `collected_amount` وتكامل شركات الشحن وأسباب الإلغاء (تيست/مكرر) — كلها خصائص سوق COD.
- حدود النطاق (خارج النطاق) مستنتجة من **غياب** الشاشات/الحقول، لا من نفي صريح؛ لذا بعضها 🟡.

---

# Confidence Level

| الجانب | الثقة |
|-------|------|
| هوية النظام كـ SaaS متعدد المستأجرين (CRM+OMS+ERP خفيف) | ✅ مؤكد |
| قائمة الوحدات الـ14 والصفحات | ✅ مؤكد |
| نموذج التسعير بالخطط + company_features | ✅ مؤكد |
| الجمهور المستهدف التجاري | 🟡 مرجّح |
| تصنيف النظام كـ "COD Commerce Platform" | 🟡 مرجّح |
| حدود النطاق (Out of Scope) | ✅/🟡 حسب البند |
| نموذج تعدد المستأجرين (Shared Schema + RLS) | 🟡 مرجّح |
| الأرقام المرجعية | ✅ مؤكد (لحظة التحليل) |

---

READY FOR NEXT FILE
