# وثيقة التحليل والهندسة العكسية الكاملة لنظام OrdersFlow

> **الرابط:** https://app.ordersflow.site
> **الشركة المستخدَمة في التحليل:** Cadeau Egypt (متجر هدايا/ورد/عطور — مصر)
> **تاريخ التحليل:** 26 يوليو 2026
> **طريقة التحليل:** استكشاف كامل للواجهة + اعتراض طلبات الشبكة (Network) لاستنتاج قاعدة البيانات الفعلية.

## دليل تصنيف الثقة المستخدم في الوثيقة

| الرمز | المعنى | المصدر |
|------|--------|--------|
| ✅ **مؤكد** | تم رؤيته مباشرة في الواجهة أو في طلبات الشبكة الفعلية | مشاهدة مباشرة |
| 🟡 **مرجّح** | استنتاج قوي مبني على أدلة غير مباشرة | استنتاج من السلوك/التسمية |
| ⚪ **تخمين** | افتراض معقول بدون دليل مباشر | خبرة معمارية |

---

# 1) الهيكل العام للنظام

| البند | التفصيل | الثقة |
|------|---------|------|
| **نوع النظام** | نظام SaaS متعدد المستأجرين (Multi-tenant) لإدارة الطلبات والعملاء — CRM + OMS (Order Management System) + جزء ERP خفيف (مخزون/موردين/ماليات) | ✅ مؤكد |
| **الفكرة الأساسية** | إدارة دورة حياة الطلب من الإنشاء (يدوي/استيراد/متجر) حتى التسليم والتحصيل، مع إدارة المخزون والفريق والماليات والشحن عبر شركات التوصيل | ✅ مؤكد |
| **الجمهور المستهدف** | تجار التجزئة الصغار والمتوسطون في مصر/الشرق الأوسط الذين يبيعون عبر السوشيال ميديا (انستجرام/فيسبوك/واتساب) ويعتمدون على الدفع عند الاستلام (COD) | 🟡 مرجّح |
| **المشكلة التي يحلها** | تشتت إدارة الطلبات بين واتساب/إكسيل/ورق؛ يوحّد: استقبال الطلب، تأكيده، تجهيزه، شحنه عبر شركة توصيل، تحصيل قيمته، وحساب الربحية | ✅ مؤكد |
| **نموذج التسعير** | اشتراك شهري بخطط (شوهدت خطة "الاحترافي" مع تاريخ انتهاء وأيام متبقية وزر "عرض الخطط") | ✅ مؤكد |
| **اللغة والعملة** | عربي (RTL) + إنجليزي؛ الجنيه المصري (ج.م. / EGP)؛ بادئة دولة قابلة للضبط (20 لمصر) | ✅ مؤكد |

## الوحدات الأساسية (Modules)

1. **Dashboard** — لوحة المؤشرات ✅
2. **Orders** — الطلبات (القلب النابض للنظام) ✅
3. **Bulk Shipment** — إنشاء الشحنات بالجملة ✅
4. **Customers** — العملاء ✅
5. **Team & Roles** — الفريق والصلاحيات ✅
6. **Inventory (Products)** — المنتجات والمخزون + المتغيرات ✅
7. **Warehouses** — المستودعات وتحويلات المخزون ✅
8. **Expenses** — المصروفات ✅
9. **Analytics** — التحليلات والتقارير ✅
10. **Shipping** — الشحن (شركات توصيل + مناطق + تسوية) ✅ (شاشة التسوية معطوبة حالياً)
11. **Finances** — الموردون + أوامر الشراء + الإقفال الشهري + المركز النقدي ✅
12. **Settings** — الإعدادات (7 تبويبات) ✅
13. **Notifications** — الإشعارات (Web Push) ✅
14. **Multi-Company** — تعدد الشركات (إنشاء/انضمام/تبديل) ✅

---

# 2) البنية المعمارية (Architecture)

## المكدّس التقني المكتشف (مؤكد من الشبكة و localStorage)

| الطبقة | التقنية | الدليل | الثقة |
|-------|---------|--------|------|
| **الواجهة الأمامية** | React (SPA) مبني بأداة Vite مع تقسيم الحزم (Code-splitting) | أسماء الحزم `ShippingSettlement-*.js`, `BulkShipmentPage-*.js`, `ui-*.js` | ✅ مؤكد |
| **مكوّنات الواجهة** | Radix UI / shadcn (tabs, dialog, combobox بنمط radix) + Tailwind | خصائص `data-radix-*`, أنماط الفئات | ✅ مؤكد |
| **الخلفية / قاعدة البيانات** | **Supabase** (PostgreSQL + PostgREST) — مشروع `gvvzqqcukynhapdgvcmj` | مفتاح `sb-gvvzqqcukynhapdgvcmj-auth-token` + مسارات `/rest/v1/...` | ✅ مؤكد |
| **المصادقة** | Supabase Auth (GoTrue) — JWT access token | بنية توكن الجلسة | ✅ مؤكد |
| **منطق الأعمال المعقّد** | دوال قاعدة بيانات (RPC / Postgres Functions) | `/rest/v1/rpc/get_analytics_summary` وغيرها | ✅ مؤكد |
| **تكاملات الشحن** | Edge Functions + Webhooks لاستقبال تحديثات شركات التوصيل | سجل نشاط "Webhook"/"Create Shipment" في تبويب الشحن | ✅ مؤكد |
| **الإشعارات** | Web Push Notifications (إشعارات الجهاز حتى عند إغلاق التطبيق) | نص إعدادات الإشعارات | ✅ مؤكد |
| **التتبّع/التحليلات** | إرسال أحداث لخدمة خارجية (`mpc-prod-*.run.app/events`) | طلب POST مرصود | ✅ مؤكد |

## نمط العزل بين المستأجرين

- كل جدول يحمل `company_id`، وكل استعلام يُفلتر به: `?company_id=eq.<uuid>` ✅ مؤكد.
- المستخدم يُخزَّن `currentCompanyId` في localStorage ويمكنه التبديل بين شركات متعددة ✅ مؤكد.
- 🟡 مرجّح: العزل مؤمّن على مستوى الصف (Row Level Security) في Postgres حسب عضوية `company_members`.

## حالة واجهة محفوظة محلياً (localStorage) ✅

`theme` (فاتح/داكن)، `currentCompanyId`، `app-language`، `ordersV2FilterState`، `ordersV2ViewMode`.
> ملاحظة: تسمية "ordersV2" 🟡 تشير إلى أن وحدة الطلبات أُعيد بناؤها (إصدار ثانٍ).

---

# 3) تحليل كل صفحة على حدة

## 3.1 لوحة التحكم — Dashboard (`/`)

- **الهدف:** نظرة عامة سريعة على أداء العمل.
- **بطاقات KPI (4):** إجمالي الطلبات (203)، صافي الدخل (57,467 ج.م.)، المخزون (564)، أعضاء الفريق (9) — مع نسبة تغيّر أسبوعية (+12% / +8%). ملاحظة: نسب التغيّر 🟡 قد تكون ثابتة/تجريبية (Placeholder).
- **الطلبات حسب الحالة:** شبكة عدّادات لكل الحالات الـ12.
- **الطلبات الأخيرة:** آخر 5 طلبات (اسم العميل، التاريخ، القيمة، الحالة).
- **تنبيهات المخزون المنخفض:** قائمة مطوّلة (548 عنصر) لكل منتج نفد أو اقترب من الحد الأدنى (اسم المنتج، المستودع، الكمية المتبقية، الحد الأدنى).
- **العمليات:** عرض فقط (تنقّل للتفاصيل). لا نماذج إدخال.
- **Workflow:** نقطة انطلاق يومية → النقر ينقل للطلب/المنتج.

## 3.2 الطلبات — Orders (`/orders`)

**الهدف:** إدارة وتتبع كل الطلبات (الوحدة الأهم).

**العناصر وأدوات التحكم:**
- بحث نصّي: "بالاسم أو الجوال أو الحساب".
- تبديل العرض: **جدول / بطاقات**.
- أزرار: التصنيفات (Labels)، طباعة، تصدير، **إنشاء طلب**.
- **فلتر التاريخ حسب:** تاريخ الإنشاء / تاريخ تغيير الحالة.
- **نطاقات جاهزة:** اليوم / هذا الأسبوع / هذا الشهر / الشهر الماضي / نطاق مخصص (Date Range Picker).
- **فلتر المُسنَد إليه:** جميع الأعضاء / عضو محدد.
- **فلتر التصنيفات:** جميع التصنيفات / تصنيف محدد.
- زر "تم إنشاء الشحنة" (فلتر إضافي 🟡).
- **تبويبات الحالة (12) مع عدّادات:** جديد، جاري التأكيد، قيد المعالجة، ناقص، جاهز، تم الشحن، تم التسليم، مكتمل، مؤجل، ملغي، مرتجع، مستبدل + "الكل".

**أعمدة الجدول:** ☐(تحديد) | رقم الطلب | العميل (اسم+هاتف) | المنتجات (عدد) | الإجمالي (+ الخصم إن وُجد بالسالب) | الدفع | الحالة | التصنيفات | مسند إلى (Assign) | التاريخ | الإجراءات.

**العمليات الممكنة:**
- تحديد جماعي (Checkbox) لعمليات دفعية 🟡 (تغيير حالة/إسناد جماعي).
- تغيير الحالة عبر Combobox داخل الصف.
- إسناد لعضو فريق.
- فتح تفاصيل الطلب (Sheet جانبي).
- طباعة/تصدير القائمة المفلترة.

**ترقيم الصفحات:** يُحمّل حتى 1000 صف للفترة المحددة (limit=1000) ✅.

### نافذة "إنشاء طلب جديد" (Dialog) ✅
حقول مؤكدة:
- **لصق ذكي (Smart Paste):** لصق نص غير منظّم → استخراج بيانات الطلب تلقائياً (ميزة مميزة).
- **المستودع** (افتراضي: المستودع الرئيسي).
- **معلومات العميل:** رقم الهاتف*، اسم العميل*، رقم هاتف ثانٍ (اختياري).
- **موقع التسليم:** المحافظة/الولاية* (قائمة)، المدينة، عنوان الشارع (تفاصيل المبنى/الطابق/الشقة).
- **وسائل التواصل:** المنصة (قائمة)، اسم المستخدم.
- **المنتجات:** "طلب سريع" + "إضافة منتج" (اختيار من الكتالوج بكمية وسعر؛ قوائم متتالية للمتغيرات).
- **الشحن والدفع:** تكلفة الشحن، الخصم، حالة الدفع (افتراضي: غير مدفوع).
- **ملاحظات.**
- أزرار: إلغاء / إنشاء طلب.

### لوحة تفاصيل الطلب (Sheet) — 4 تبويبات ✅
- **رأس اللوحة:** رقم الطلب + الحالة + "نسخ التفاصيل" + "تعديل".
- **تبويب التفاصيل:** معلومات العميل، منتجات الطلب (كمية × سعر)، المجموع الفرعي، الشحن، الإجمالي، حالة الدفع، **المبلغ المحصّل** (مع ملاحظة: "المبلغ الفعلي المستلم بعد خصم رسوم شركة الشحن")، الملاحظات، **التعليقات** (إضافة تعليق داخلي).
- **تبويب سجل النشاط:** تاريخ الإنشاء/التحديث + سجل تغييرات الحالة والإجراءات.
- **تبويب الحالة:** حالة الطلب، **حالة المتابعة** (قيد الانتظار…)، **مسند إلى (مبيعات)**، **التصنيفات** (إضافة/إزالة: مستعجل، مكرر…).
- **تبويب الشحن:** حالة الشحنة (حالياً "لم يتم إنشاء شحنة بعد") → إنشاء/تتبع بوليصة الشحن.

### وضع التعديل (Edit) ✅
يفتح نفس الحقول قابلة للتحرير + حقل "المبلغ المحصّل" + زر "حفظ التغييرات".

**Validation المرصودة:** الحقول ذات النجمة (الهاتف/الاسم/المحافظة) إلزامية 🟡.

## 3.3 الشحن بالجملة — Bulk Shipment (`/orders/bulk-shipment`)
- **الهدف:** إنشاء شحنات لعدة طلبات دفعة واحدة وطباعة بوالصها.
- **العناصر:** اختيار حالة الطلب (قائمة) → "تحميل الطلبات (حتى 500)" → "إنشاء شحنات (N)" → "تنزيل بوالص الشحن".
- **Workflow:** فلترة بالحالة → تحميل → تحديد → إنشاء عبر شركة التوصيل → تنزيل PDF للبوالص. ✅

## 3.4 العملاء — Customers (`/customers`)
- **الهدف:** قاعدة بيانات العملاء (1053 عميلاً).
- **الفلاتر:** بحث (اسم/هاتف/حساب)، **النوع** (جميع الأنواع / Regular / …)، **الحالة** (نشط…)، **نطاق المبلغ** (حد أدنى/أقصى للإيرادات).
- **الأعمدة:** الاسم | الهاتف | النوع (Regular) | الطلبات (عدد) | الإيرادات | الحالة (نشط) | الإجراءات (تعديل/حذف/تغيير حالة 🟡).
- **تصدير** متاح. **ترقيم:** 10/صفحة.
- **صفحة تفاصيل العميل** `/customers/{uuid}` ✅: رأس (اسم/هاتف/نوع) + بطاقات (إجمالي الطلبات، عدد المُسلّم، إجمالي الإيرادات، متوسط قيمة الطلب، نوع العميل) + معلومات (العنوان، المدينة، الحي، السوشيال، الملاحظات) + **سجل الطلبات** (رقم، تاريخ، حالة، عناصر، إجمالي).

## 3.5 الفريق — Team (`/team`)
- **الهدف:** إدارة الأعضاء وأدوارهم.
- **رمز الدعوة (Invite Code):** `a8f05377` يُشارَك للانضمام للشركة ✅.
- **دليل صلاحيات الأدوار:** لوحة قابلة للطي تشرح ما يراه/يغيّره كل دور من حالات الطلبات (انظر قسم 6).
- **قائمة الأعضاء:** الأحرف الأولى + الاسم + الدور + عدد الطلبات المُسندة.
- 🟡 العمليات المرجّحة: تغيير دور عضو، إزالة عضو، إعادة توليد رمز الدعوة.

## 3.6 المنتجات/المخزون — Inventory (`/inventory`)
- **الهدف:** تتبع وإدارة مخزون المنتجات (564 منتجاً).
- **العناصر:** محدّد المستودع (مع نجمة الافتراضي)، **إضافة منتج**، بحث، "إخفاء المنتجات غير المتوفرة"، تبديل جدول/شبكة.
- **الأعمدة:** اسم المنتج | رمز المنتج (SKU) | السعر | التكلفة | المخزون | مُباع | الحالة (نفذ/متبقٍ N) | الإجراءات.
- **إجراءات الصف:** عرض التفاصيل، **إدارة المتغيرات**، تعديل/حذف 🟡.
- **ترقيم:** 10/صفحة (لكن يُحمّل حتى 10000 داخلياً في بعض الاستعلامات).

### نافذة "إضافة منتج" ✅
الاسم، رمز المنتج (اختياري)، السعر (EGP)، التكلفة (EGP)، المخزون، **حد المخزون المنخفض**، صورة (تُضغط تلقائياً)، مفتاح **"السماح بالبيع عند نفاذ المخزون"**.

### نافذة "إدارة المتغيرات" (Variants) ✅
- خصائص المتغير (لون/مقاس/عطر) تُفعّل **قوائم متتالية** عند إنشاء الطلب.
- جدول متغيرات: اسم المتغير*، السعر*، التكلفة، المخزون، رمز المنتج.
- "إضافة متعددة سريعة" (سعر/تكلفة/مخزون افتراضي) + حفظ الكل.

## 3.7 المستودعات — Warehouses (`/warehouses`)
- **الهدف:** إدارة مواقع التخزين (3 مستودعات نشطة).
- **التبويبات:** المستودعات النشطة / المؤرشفة / **سجل التحويلات**.
- **العمليات:** إضافة مستودع، **تحويل المخزون** بين المستودعات، تعيين كافتراضي، أرشفة، تعديل.
- **بيانات المستودع:** الاسم، العنوان، عدد المنتجات (المستودع الرئيسي 561، مقر الشركة 3، مصنع سيد 0).

## 3.8 المصروفات — Expenses (`/expenses`)
- **الهدف:** تتبع مصروفات العمل.
- **بطاقات مجمّعة حسب الفئة:** إجمالي، الرواتب، الصيانة، المواد، الأصول، إعلانات، أخرى.
- **الفلاتر:** اليوم/أسبوع/شهر/الشهر الماضي/مخصص، **الفئة**، **نطاق المبلغ** (أدنى/أقصى).
- **الأعمدة:** التاريخ | الفئة | ملاحظات | المبلغ | الإجراءات (تعديل/حذف).
- **إضافة مصروف** (الفئة، المبلغ، التاريخ، ملاحظات) ✅.

## 3.9 التحليلات — Analytics (`/analytics`)
- **أدوات:** فلاتر التاريخ + **تصدير Excel**.
- **5 تبويبات:**

**أ) الأعمال (Business):** بطاقات — إجمالي الإيرادات، المبلغ المحصّل، إجمالي المصروفات، إجمالي رسوم الشحن المضافة، تكلفة البضائع المباعة (COGS)، **صافي الدخل = المحصّل − المصروفات − تكلفة البضائع**، إجمالي الطلبات، العملاء الفريدون، العملاء العائدون (%)، العملاء الجدد. رسوم: المبيعات حسب الشهر (12 شهراً)، الطلبات حسب الحالة، المصروفات حسب الفئة، أفضل العملاء، أفضل المنتجات.

**ب) المنتجات (Products):** إجمالي الوحدات المباعة، الأكثر مبيعاً، أعلى معدل إرجاع، الأكثر إلغاءً، الطلب العالي (عملاء فريدون)، **تحليل المنتجات الملغاة مع أسباب الإلغاء** (تيست/متكرر/بدون سبب).

**ج) المخزون (Inventory):** إجمالي قيمة المخزون (بالتكلفة)، إجمالي الوحدات، **تفصيل التكاليف حسب المستودع**.

**د) الموظفون (Employees):** لكل عضو — معدل التحويل %، عدد الإجراءات، الإيراد المُحقّق، (أنشأ / مُسند إليه / تم التسليم / مكتمل / ملغي / مرتجع) + "تحميل التقرير".

**هـ) الربحية (Profitability):** **المركز النقدي** (النقد في اليد = المحصّل − المدفوع للموردين − المصروفات؛ المستحق للموردين) + **قائمة الأرباح والخسائر الشهرية** (الإيرادات/تكلفة البضائع/الإعلانات/المصروفات/صافي الربح) + طباعة PDF.

## 3.10 تسوية الشحن — Shipping Settlement (`/shipping/settlement`)
- **الهدف المرجّح:** تسوية المبالغ المحصّلة عبر شركات الشحن (COD) مقابل ما استُلم فعلياً وخصم رسوم الشحن.
- **الحالة:** ⚠️ **معطوبة حالياً** — الصفحة تتعطل عند التحميل بخطأ `React error #310` (خطأ ترتيب Hooks في مكوّن `ShippingSettlement`). صفحة بيضاء. ✅ مؤكد (خطأ برمجي فعلي).

## 3.11 الماليات — Finances

### `/finances` (نظرة عامة)
🟡 يعيد التوجيه فعلياً إلى لوحة التحكم في التنقّل المباشر (قد يكون فهرساً يعيد التوجيه).

### الموردون — `/finances/suppliers` ✅
- بطاقات: إجمالي الموردين (4)، إجمالي المستحقات، "عرض المؤرشف".
- الأعمدة: الاسم | النوع (محلي/مستورد 🟡) | رقم الهاتف | المتبقي (رصيد مستحق) | الإجراءات.
- بحث + أرشفة.

### أوامر الشراء — `/finances/purchase-orders` ✅
- بطاقات: إجمالي الأوامر، المتبقي، في انتظار الاستلام.
- فلاتر: كل المدفوعات / كل الاستلامات.
- الأعمدة: رقم الأمر (PO-YYYY-NNN) | المورد | التاريخ | الإجمالي | المتبقي | **الدفع** (غير مدفوع/مدفوع بالكامل) | **الاستلام** (لم يُستلم/جزئي/مستلم بالكامل).
- **تفاصيل الأمر:** بنود (po_items) + مدفوعات (po_payments) + **سجلات الاستلام** (po_receiving_logs) — الاستلام يزيد المخزون 🟡.

### الإقفال الشهري — `/finances/close` ✅
- **الهدف:** مراجعة وإغلاق الحسابات شهرياً (يجب بالترتيب الزمني).
- **قائمة تحقق قبل الإقفال:** تحديث كل الطلبات (يظهر "14 طلب لا يزال مفتوحاً")، استلام كل أوامر الشراء، تسجيل مصروفات الشهر.
- **ملخص الشهر:** الإيرادات، تكلفة البضائع، المصروفات، صافي الربح.
- **السجل:** الأشهر المقفلة (`month_closes`) + رأس المال الختامي (`closing_capital`).

## 3.12 الإعدادات — Settings (`/settings`)
7 تبويبات ✅:
1. **عام:** اللغة (English/العربية) + **الاشتراك** (الخطة الحالية "الاحترافي"، تاريخ الانتهاء، الأيام المتبقية، عرض الخطط).
2. **استيراد البيانات:** استيراد الطلبات + استيراد المنتجات من Excel/CSV مع **ربط الأعمدة وقيم الحالة**.
3. **الأمان:** تغيير كلمة المرور + **طلب حذف الحساب** نهائياً.
4. **الإشعارات:** Web Push — تُفعّل عند: "تعيين طلب جديد"، "تعيين توصيل".
5. **واتساب:** بادئة الدولة + **قوالب رسائل** قابلة لإعادة الاستخدام (قالب تأكيد الأوردر…).
6. **الشحن:** **مناطق الشحن** (سعر لكل محافظة/منطقة) + **شركات الشحن** (Bosta متصلة، QPExpress, ShopShip, J&T Express, Speedaf, Accurate Logistics, Mylerz, LogesTechs) + **مكاتب الشحن** (تظهر على البوالص) + سجل نشاط (Webhook/Create Shipment).
7. **الأسباب:** **أسباب الإلغاء** (لم يُدفع العربون، العميل لا يرد، مفيش وقت، الأوردر اتأخر، العميل هيسافر، تيست، متكرر) + **أسباب الإرجاع** — قوائم قابلة للتخصيص.

## 3.13 شريط علوي + تعدد الشركات ✅
- جرس **الإشعارات**، مبدّل **الثيم** (فاتح/داكن)، قائمة الشركة.
- **قائمة الشركة:** الشركة الحالية (Cadeau Egypt) + **إنشاء شركة** + **الانضمام لشركة** (تعدد المستأجرين على مستوى المستخدم).

---

# 4) تحليل قاعدة البيانات (مستنتَج من طلبات PostgREST الفعلية)

> جميع الجداول أدناه **مؤكدة ✅** ما لم يُذكر خلاف ذلك؛ أسماؤها ظهرت في مسارات `/rest/v1/<table>`. الأعمدة المؤكدة ظهرت في `select`/الواجهة؛ غير ذلك 🟡/⚪.

### `companies` — الشركات (المستأجر الجذر) ✅
- **الغرض:** كيان الشركة/المتجر. PK: `id (uuid)`. أعمدة: `name`, `country`, `currency`, `country_prefix` 🟡, `created_at`.

### `company_members` — عضوية الفريق ✅
- **الغرض:** ربط المستخدمين بالشركات وأدوارهم. أعمدة مؤكدة: `company_id (FK→companies)`, `user_id (FK→profiles/auth)`, `role`, `status`. PK ⚪ (company_id+user_id).

### `company_features` — مفاتيح تفعيل الميزات ✅
- **الغرض:** تفعيل/تعطيل ميزات لكل شركة (Feature Flags). أعمدة: `company_id`, وأعلام لكل ميزة ⚪.

### `profiles` — ملفات المستخدمين ✅
- `id (=auth.users.id)`, `full_name`, `avatar` 🟡, بيانات العرض.

### `customers` — العملاء ✅
- **الغرض:** قاعدة العملاء. أعمدة مرجّحة: `id`, `company_id`, `name`, `phone`, `phone2`, `type (Regular/…)`, `status (active/…)`, `address`, `city`, `neighborhood`, `governorate`, `social_platform`, `social_username`, `notes`, `total_orders`🟡, `total_revenue`🟡, `created_at`. Index مرجّح: `(company_id, phone)`.

### `orders` — الطلبات ✅ (الجدول المركزي)
- أعمدة مؤكدة/مرجّحة: `id`, `company_id`, `customer_id`🟡, `order_number`, `customer_name`, `customer_phone`, `customer_phone2`, `governorate`, `city`, `street_address`, `neighborhood`, `social_platform`, `social_username`, `status` (enum بـ12 قيمة)، `follow_up_status`, `assigned_to (FK→company_members)`, `inventory_id (FK→inventories)`🟡, `subtotal`, `shipping_cost`, `discount`, `total`, `payment_status`, `collected_amount`, `notes`, `cancel_reason_id`🟡, `created_at`, `updated_at`, `status_changed_at`🟡.
- **Indexes مرجّحة:** `(company_id, status)`, `(company_id, created_at)`, `(assigned_to)`.

### `order_items` — بنود الطلب ✅
- `id`, `order_id (FK→orders)`, `product_id (FK→products)`, `product_name`, `quantity`, `price`.

### `order_labels` — التصنيفات/الوسوم ✅
- `id`, `company_id`, `name`, `color`.

### `order_label_assignments` — ربط الطلب بالتصنيفات (M:N) ✅
- `order_id (FK→orders)`, `label_id (FK→order_labels)`.

### `order_reasons` — أسباب الإلغاء/الإرجاع ✅
- `id`, `company_id`, `type` (cancel/return), `label`, `created_at`.

### `whatsapp_confirmations` — تأكيدات واتساب ✅
- `id`🟡, `order_id (FK→orders)`, `status`, `sent_at`.

### `products` — المنتجات والمتغيرات ✅
- `id`, `company_id`, `name`, `sku`, `price`, `cost`, `low_stock_threshold`, **`parent_product_id (FK→products, self)`** (المتغيرات أبناء للمنتج الأب)، `variant_name`, `allow_oversell`🟡, `image_url`🟡, `created_at`.
- **Index:** `(company_id)`, `(parent_product_id)`.

### `inventories` — المستودعات ✅
- `id`, `company_id`, `name`, `address`🟡, `is_default`, `is_archived`🟡, `created_at`.

### `inventory_stock` — المخزون لكل مستودع ✅
- `product_id (FK→products)`, `inventory_id (FK→inventories)`, `on_hand`, `committed`, `quantity`. PK مرجّح: `(inventory_id, product_id)`.

### `stock_transfers` — تحويلات المخزون ✅
- `id`, `company_id`, `from_inventory_id (FK→inventories)`, `to_inventory_id (FK→inventories)`, `created_at`, بنود التحويل ⚪ (`stock_transfer_items`).

### `suppliers` — الموردون ✅
- `id`, `company_id`, `name`, `type` (local/import)، `phone`, `balance/remaining`🟡, `is_archived`.

### `purchase_orders` — أوامر الشراء ✅
- `id`, `company_id`, `supplier_id (FK→suppliers)`, `po_number`, `total_amount`, `paid_amount`, `payment_status`🟡, `receiving_status`, `created_at`.

### `po_items` — بنود أمر الشراء ✅
- `id`, `purchase_order_id (FK)`, `product_id (FK→products)`, `quantity`🟡, `cost`🟡.

### `po_payments` — مدفوعات أوامر الشراء ✅
- `id`, `purchase_order_id (FK)`, `company_id`, `amount`, `payment_date`.

### `po_receiving_logs` — سجلات استلام أوامر الشراء ✅
- `id`, `purchase_order_id (FK)`, كمية مستلمة + تاريخ + المستودع المستهدف ⚪ (يزيد `inventory_stock`).

### `expenses` — المصروفات ✅
- `id`, `company_id`, `amount`, `category` (الرواتب/الصيانة/المواد/الأصول/إعلانات/أخرى)، `notes`, `expense_date`.

### `month_closes` — الإقفالات الشهرية ✅
- `id`🟡, `company_id`, `month (date)`, `status` (closed)، `closing_capital`.

### `capital_log` — سجل رأس المال ✅
- `id`🟡, `company_id`, `amount`, `log_date`.

### `notifications` — الإشعارات ✅
- `id`, `user_id`, `type`🟡, `payload`🟡, `read`🟡, `created_at`.

### `shipping_offices` — مكاتب الشحن ✅
- `id`, `company_id`, `name`, `printed_name`.

### جداول مرجّحة/مخمّنة (لم تُرصد مباشرة لكنها ضرورية) 🟡/⚪
- `shipments` — الشحنات (order_id, carrier, tracking_number, status, cod_amount, label_url) 🟡.
- `shipping_zones` — مناطق الشحن (company_id, governorate/region, price) 🟡.
- `carrier_connections` / `shipping_integrations` — ربط شركات التوصيل (company_id, carrier, credentials, is_connected) 🟡.
- `shipping_settlements` — تسويات الشحن (مقابل شاشة settlement) 🟡.
- `activity_log` / `order_activities` — سجل نشاط الطلب 🟡.
- `order_comments` — تعليقات الطلب الداخلية 🟡.
- `subscriptions` / `plans` — الاشتراكات والخطط 🟡.
- `auth.users` — مستخدمو Supabase Auth ✅ (مدار من Supabase).

### دوال قاعدة البيانات (RPC) المؤكدة ✅
`get_company_members_with_profiles` · `get_order_status_counts` · `get_inventory_products` · `get_analytics_summary`.
🟡 مرجّحة إضافية: `close_month`, `create_shipment`, `smart_paste_parse`.

---

# 5) نموذج العلاقات (ERD مستنتَج)

```
companies (1) ──< company_members >── (M) profiles/auth.users
companies (1) ──< customers
companies (1) ──< orders                orders (M) >── (1) customers
                                        orders (M) >── (1) company_members [assigned_to]
orders (1) ──< order_items >── (M) products
orders (M) ──< order_label_assignments >── (M) order_labels
orders (1) ──< whatsapp_confirmations
orders (1) ──< shipments (مرجّح) >── (1) carrier
orders (M) >── (1) order_reasons [cancel_reason]

companies (1) ──< products
products (1) ──< products [parent_product_id → المتغيرات]
products (1) ──< inventory_stock >── (M) inventories
inventories (1) ──< stock_transfers >── (1) inventories [from/to]

companies (1) ──< suppliers
suppliers (1) ──< purchase_orders
purchase_orders (1) ──< po_items >── (1) products
purchase_orders (1) ──< po_payments
purchase_orders (1) ──< po_receiving_logs → (يزيد) inventory_stock

companies (1) ──< expenses
companies (1) ──< month_closes
companies (1) ──< capital_log
companies (1) ──< shipping_offices / shipping_zones / carrier_connections (مرجّح)
profiles (1) ──< notifications
```

**العلاقات المحورية:**
- كل شيء يتفرّع من `companies` (عزل المستأجرين).
- `orders` هو المركز الذي يربط العملاء + المنتجات + الفريق + الشحن + التصنيفات + المتابعة.
- المخزون علاقة (منتج × مستودع) عبر `inventory_stock`.
- الماليات: الطلبات (تحصيل) + المصروفات + أوامر الشراء (مدفوعات) → المركز النقدي والإقفال الشهري.

---

# 6) تحليل الصلاحيات (مصفوفة الأدوار — مؤكدة من "دليل صلاحيات الأدوار") ✅

النظام يبني الصلاحيات على **رؤية/تغيير حالات الطلبات** لكل دور. الأدوار التسعة:

| الدور | يرى الحالات | يغيّر إلى | ملاحظة |
|------|-------------|-----------|--------|
| **مسؤول (Admin)** | جميع الحالات | جميع الحالات | صلاحية كاملة + إدارة كل شيء |
| **مدير (Manager)** | جميع الحالات | جميع الحالات | صلاحية كاملة |
| **موظف (Employee)** | جميع الحالات | جميع الحالات | صلاحية كاملة |
| **موديريتور (Moderator)** | جديد، جاري التأكيد، قيد المعالجة، ملغي، مؤجل، مستبدل، ناقص، جاهز، تم الشحن | نفسها | لا يرى: تم التسليم/مكتمل/مرتجع |
| **كول سنتر (Call Center)** | مثل الموديريتور | مثله | تركيز على التأكيد الهاتفي |
| **موظف تجهيز (Fulfillment)** | مثل الموديريتور + **مرتجع** | نفسها | مسؤول التغليف/التجهيز |
| **موظف شحن (Shipping)** | الكل حتى: تم التسليم، مكتمل، مرتجع | نفسها | يدير مرحلة الشحن والتسليم |
| **مندوب توصيل (Delivery)** | جاهز، تم الشحن، تم التسليم | جاهز، تم الشحن، تم التسليم، مؤجل، ملغي | رؤية محدودة جداً |
| **مدير المستودع (Warehouse)** | — | — | **لا صلاحية وصول للطلبات** (مخزون فقط) |

**استنتاجات إضافية للصلاحيات:**
- من يستطيع **الإضافة/التعديل/الحذف الكامل وإدارة المستخدمين/الفروع/الإعدادات/الماليات**: الأدوار كاملة الصلاحية (مسؤول/مدير/موظف) ✅/🟡.
- **رمز الدعوة** وإدارة الأعضاء: على الأرجح للمسؤول 🟡.
- **التصدير**: متاح في صفحات الطلبات/العملاء/التحليلات 🟡 للأدوار كاملة الصلاحية.
- **الإقفال الشهري**: 🟡 مقصور على مسؤول/محاسب.
- ملاحظة: الدور "محاسب (Accountant)" ظهر في قائمة الأعضاء لكنه لم يُدرَج في دليل حالات الطلبات → صلاحياته على الأرجح مالية (لا حالات طلبات) 🟡.

---

# 7) تحليل سير العمل (Workflows)

## 7.1 دورة حياة الطلب (الأساسية) ✅
```
استقبال الطلب (يدوي / لصق ذكي / استيراد / متجر)
      ↓  [جديد]
تأكيد العميل (كول سنتر/واتساب — دفع عربون؟)
      ↓  [جاري التأكيد]
معالجة/تجهيز (خصم المخزون committed)
      ↓  [قيد المعالجة → ناقص إن نقص صنف → جاهز]
إنشاء الشحنة (عبر شركة توصيل — بوليصة)
      ↓  [تم الشحن]
تسليم عبر المندوب (COD)
      ↓  [تم التسليم]
تحصيل المبلغ + خصم رسوم الشحن (collected_amount)
      ↓  [مكتمل]
```
**مسارات فرعية:** مؤجل (يعاد جدولته) · ملغي (بسبب من قائمة الأسباب) · مرتجع · مستبدل.

## 7.2 دورة المخزون والشراء ✅
```
منتج ناقص → أمر شراء (PO) لمورد → دفع (po_payments) → استلام (po_receiving_logs)
→ زيادة inventory_stock → (اختياري) تحويل بين المستودعات (stock_transfers)
```

## 7.3 الدورة المالية والإقفال ✅
```
طلبات محصّلة + مصروفات + مدفوعات موردين → المركز النقدي (النقد في اليد)
→ نهاية الشهر: قائمة تحقق (طلبات محدّثة/PO مستلمة/مصروفات مسجّلة)
→ إقفال الشهر بالترتيب (month_closes + closing_capital) → قفل البيانات
```

## 7.4 دورة إعداد الشركة (Onboarding) 🟡
```
إنشاء/الانضمام لشركة → إعداد المستودع الافتراضي → استيراد المنتجات/الطلبات
→ ضبط مناطق الشحن وربط شركة توصيل → دعوة الفريق (رمز الدعوة) وتعيين الأدوار
→ ضبط قوالب واتساب وأسباب الإلغاء
```

## 7.5 دورة الفريق والإسناد ✅
```
طلب جديد → إسناد لعضو (assigned_to) → إشعار Web Push للعضو
→ تحديث الحالة ضمن صلاحيات دوره → قياس الأداء (تبويب الموظفون)
```

---

# 8) تحليل تجربة المستخدم (UX)

- **سهولة الاستخدام:** عالية للوحدة الأساسية (الطلبات) — تبويبات حالة بعدّادات + فلاتر غنية + عرض جدول/بطاقات. ✅
- **وضوح الواجهة:** جيد؛ RTL أصيل، تسميات عربية واضحة، بطاقات KPI مباشرة.
- **عدد الخطوات:** إنشاء الطلب مُحسّن بميزة "اللصق الذكي" (تقليل الإدخال اليدوي) — نقطة قوة كبيرة. ✅
- **سرعة الوصول:** الشريط الجانبي يجمع الوحدات مع مجموعات قابلة للطي (المخزون/الماليات) — جيد لكن يخفي روابط.
- **الألوان:** نظام ثيم فاتح/داكن مدعوم.
- **تنظيم الصفحات:** منطقي (طلبات/عملاء/مخزون/ماليات/تحليلات/إعدادات).
- **سهولة التعلّم:** "دليل صلاحيات الأدوار" داخل التطبيق يساعد — إضافة تعليمية جيدة.
- **نقاط احتكاك ملحوظة (بدون اقتراح حلول):**
  - التنقّل المباشر عبر URL للمسارات المتداخلة (finances/*) يعيد للوحة التحكم (يعمل فقط عبر التنقّل الداخلي). ✅
  - صفحة تسوية الشحن تتعطّل تماماً (شاشة بيضاء). ✅
  - بعض القيم النقدية تظهر بالدولار ($0.00) في صفحة العملاء بينما باقي النظام بالجنيه — عدم اتساق عملة. ✅
  - قائمة تنبيهات المخزون في لوحة التحكم طويلة جداً (548 عنصراً) بلا ترقيم/طيّ. ✅

## 9) تحليل واجهة المستخدم (UI / Design System)

- **الإطار:** Tailwind CSS + مكوّنات shadcn/Radix (dialog, tabs, combobox, checkbox, tablist). ✅
- **Typography:** خط يدعم العربية، أحجام هرمية للعناوين/الوصف.
- **Cards:** بطاقات KPI موحّدة بحواف مستديرة وظلال خفيفة.
- **Buttons:** أساسي/ثانوي/أيقوني (نمط `inline-flex … rounded-md`).
- **Tables:** صفوف بحدود سفلية + Hover + عمود إجراءات محاذى للنهاية (RTL) + Checkbox تحديد.
- **Colors:** متغيّرات ثيم (primary/muted/background) + ألوان دلالية للحالات + ألوان مخصّصة للتصنيفات (`order_labels.color`).
- **Icons:** أيقونات SVG (Lucide 🟡 — ظهر `file-down`).
- **Dialogs/Sheets:** حوارات منبثقة للنماذج + لوحة جانبية (Sheet) لتفاصيل الطلب بتبويبات.
- **Responsive:** SPA بتصميم متجاوب 🟡؛ إشعارات جهاز تعمل كتطبيق شبه-أصلي (PWA مرجّح، بسبب Web Push وضغط الصور محلياً).

---

# 10) تحليل الأداء

- **أماكن قد تسبب بطئاً (✅ مرصودة):**
  - `products?...&limit=10000` — تحميل حتى 10000 منتج دفعة واحدة (شاشة المستودعات).
  - `orders?...&limit=1000` — تحميل حتى 1000 طلب للفترة.
  - لوحة التحكم تجلب كل تنبيهات المخزون المنخفض (548) بلا ترقيم.
- **الترقيم (Pagination) موجود في:** العملاء (10/صفحة)، المنتجات (10/صفحة) ✅. غائب في: تنبيهات لوحة التحكم، بعض القوائم المجمّعة.
- **أماكن تحتاج Lazy Loading (🟡):** قوائم الطلبات/المنتجات الكبيرة، صور المنتجات.
- **أماكن تحتاج Cache (🟡):** `company_features`, `order_labels`, `order_reasons`, `inventories`, قائمة المحافظات — بيانات شبه ثابتة تُجلب مع كل تنقّل.
- **إيجابيات:** استخدام دوال RPC للتجميعات الثقيلة (عدّادات الحالة/التحليلات) بدل حسابها على العميل ✅؛ تقسيم الحزم (Code-splitting) لكل صفحة ✅؛ ضغط الصور قبل الرفع ✅.
- **عدد الطلبات المتوقع عند فتح صفحة:** 6–10 طلبات REST متوازية (بيانات + عضوية + إشعارات + ميزات + شركة) ✅.

---

# 11) تحليل الـ API (Endpoints — مستنتَجة من PostgREST) 

> النمط: `GET/POST/PATCH/DELETE /rest/v1/<table>` مع فلاتر PostgREST (`eq/in/gte/lte/not.in/order/limit/offset`). العمليات CRUD قياسية لكل جدول.

**الطلبات:** 
`GET /rest/v1/orders?select=*,order_items(*),order_label_assignments(order_labels(*)),whatsapp_confirmations(*)&company_id=eq.&status=eq.&created_at=gte.&order=created_at.desc&limit=&offset=` ✅ ·
`POST /rest/v1/orders` · `PATCH /rest/v1/orders?id=eq.` · `DELETE …` 🟡 ·
`POST /rest/v1/rpc/get_order_status_counts` ✅.

**بنود/تصنيفات:** `…/order_items`, `…/order_labels`, `…/order_label_assignments`, `…/order_reasons`, `…/whatsapp_confirmations` ✅.

**العملاء:** `GET/POST/PATCH/DELETE /rest/v1/customers?...` ✅.

**المنتجات والمخزون:** `POST /rest/v1/rpc/get_inventory_products` ✅ · `GET /rest/v1/products?...&parent_product_id=not.is.null` (المتغيرات) ✅ · `GET /rest/v1/inventory_stock?select=*,on_hand,committed,products(...)` ✅ · `GET /rest/v1/inventories?select=*,inventory_stock(id)` ✅ · `GET /rest/v1/stock_transfers?select=*,from_inventory:inventories!from_inventory_id(name),to_inventory:…` ✅.

**الموردون والشراء:** `GET /rest/v1/suppliers?...&is_archived=eq.false` ✅ · `GET /rest/v1/purchase_orders?select=*,suppliers(name),po_items(*,products(name)),po_payments(*),po_receiving_logs(*)` ✅.

**المصروفات والماليات:** `GET /rest/v1/expenses?...` ✅ · `GET /rest/v1/month_closes?...` ✅ · `GET /rest/v1/capital_log?...` ✅ · `POST /rest/v1/rpc/get_analytics_summary` ✅.

**الفريق والشركة:** `GET /rest/v1/company_members?select=company_id,role,status` ✅ · `POST /rest/v1/rpc/get_company_members_with_profiles` ✅ · `GET /rest/v1/companies?...` ✅ · `GET /rest/v1/company_features?...` ✅ · `GET /rest/v1/profiles?...` ✅.

**الإشعارات والشحن:** `GET /rest/v1/notifications?...&order=created_at.desc&limit=50` ✅ · `GET /rest/v1/shipping_offices?...` ✅ · (مرجّح) `…/shipping_zones`, `…/shipments`, ودالة/Edge Function لإنشاء الشحنة + Webhook لاستقبال تحديثات شركات التوصيل 🟡.

**المصادقة:** `POST /auth/v1/token` (تسجيل الدخول) ✅ · إدارة كلمة المرور/حذف الحساب عبر GoTrue 🟡.

---

# 12) تحليل البحث والفلاتر (لكل شاشة)

| الشاشة | البحث | الفلاتر | الترتيب | تصدير | استيراد |
|-------|------|---------|--------|-------|--------|
| الطلبات | اسم/جوال/حساب | الحالة (12)، التاريخ (نوعان)، النطاق الزمني، المُسنَد، التصنيفات، تم إنشاء الشحنة | تاريخ تنازلي | ✅ | ✅ (إعدادات) |
| العملاء | اسم/هاتف/حساب | النوع، الحالة، نطاق الإيراد | 🟡 | ✅ | — |
| المنتجات | نص | المستودع، إخفاء غير المتوفر | created_at | 🟡 | ✅ (إعدادات) |
| المصروفات | — | الفئة، التاريخ، نطاق المبلغ | تاريخ | 🟡 | — |
| الموردون | اسم/هاتف | مؤرشف | name.asc | 🟡 | — |
| أوامر الشراء | — | الدفع، الاستلام | created_at | 🟡 | — |
| التحليلات | — | التاريخ | — | ✅ Excel | — |

---

# 13) تحليل التقارير و KPIs

**KPIs مؤكدة ✅:** إجمالي الإيرادات · المبلغ المحصّل · إجمالي المصروفات · رسوم الشحن المضافة · تكلفة البضائع (COGS) · **صافي الدخل** · إجمالي الطلبات · العملاء الفريدون/العائدون/الجدد · معدل التحويل لكل موظف · قيمة المخزون · متوسط قيمة الطلب.

**الرسوم البيانية ✅:** المبيعات حسب الشهر (12 شهراً، خطي/عمودي)، الطلبات حسب الحالة (أعمدة)، المصروفات حسب الفئة (دائري)، تكاليف المخزون حسب المستودع (أعمدة).

**التقارير ✅:** قائمة الأرباح والخسائر الشهرية (P&L) بطباعة PDF · تقرير أداء الموظفين (تحميل) · تصدير Excel للتحليلات · أفضل العملاء/المنتجات · تحليل المنتجات الملغاة بالأسباب.

---

# 14) تحليل الإعدادات (ما يمكن للمستخدم تغييره)

- **الشركة:** الاسم/تعدد الشركات (إنشاء/انضمام/تبديل) ✅.
- **الفروع/المستودعات:** إضافة/أرشفة/تعيين افتراضي/تحويل مخزون ✅.
- **المستخدمون:** دعوة برمز، تعيين أدوار 🟡.
- **الصلاحيات:** عبر نظام الأدوار (9 أدوار) ✅.
- **الإشعارات:** تفعيل Web Push ومحفّزاته ✅.
- **الضرائب:** ❌ غير موجودة صراحةً (لا حقل ضريبة/VAT مرصود).
- **الفواتير:** لا وحدة فواتير رسمية؛ يوجد بوالص شحن + "نسخ التفاصيل" فقط 🟡.
- **العملة:** ثابتة (EGP) — بادئة الدولة فقط قابلة للضبط ✅ (لا مبدّل عملة كامل).
- **اللغة:** عربي/إنجليزي ✅.
- **الشحن:** مناطق + شركات توصيل + مكاتب ✅.
- **الأسباب:** الإلغاء/الإرجاع ✅.
- **واتساب:** بادئة + قوالب ✅.
- **الأمان:** كلمة المرور + حذف الحساب ✅.
- **الاشتراك:** عرض الخطة والانتهاء + عرض الخطط ✅.

---

# 15) قائمة المميزات الكاملة (Feature List)

1. تعدد الشركات/المستأجرين + رمز دعوة للانضمام ✅
2. نظام أدوار مبني على حالات الطلبات (9 أدوار) + دليل صلاحيات مدمج ✅
3. إدارة طلبات كاملة بـ12 حالة + حالة متابعة منفصلة ✅
4. **اللصق الذكي** لاستخراج الطلب من نص غير منظّم ✅
5. عرض جدول/بطاقات + فلاتر متقدمة + تبويبات حالة بعدّادات ✅
6. لوحة تفاصيل الطلب (تفاصيل/نشاط/حالة/شحن) + تعليقات داخلية ✅
7. إسناد الطلبات للأعضاء + قياس أدائهم ✅
8. تصنيفات/وسوم ملوّنة للطلبات (مستعجل/مكرر…) ✅
9. حالات دفع متعددة + **المبلغ المحصّل بعد رسوم الشحن** ✅
10. إدارة العملاء (1000+) بملف تفصيلي وسجل طلبات وKPIs ✅
11. كتالوج منتجات + **متغيرات (لون/مقاس/عطر) بقوائم متتالية** ✅
12. تتبع المخزون لكل مستودع (on_hand/committed) + حد أدنى + السماح بالبيع عند النفاد ✅
13. تعدد المستودعات + **تحويل مخزون** + أرشفة + سجل تحويلات ✅
14. المصروفات المصنّفة + بطاقات مجمّعة ✅
15. الموردون + أوامر شراء (دفع + استلام جزئي/كامل → مخزون) ✅
16. **المركز النقدي** (النقد في اليد) + المستحقات ✅
17. **الإقفال الشهري** المتسلسل بقائمة تحقق ورأس مال ختامي ✅
18. تحليلات غنية (5 تبويبات) + P&L + تصدير Excel/PDF ✅
19. **الشحن بالجملة** + طباعة بوالص (حتى 500) ✅
20. تكامل شركات التوصيل (Bosta وغيرها) + Webhooks للتتبع ✅
21. مناطق شحن بأسعار حسب المحافظة + مكاتب شحن على البوالص ✅
22. **قوالب واتساب** + بادئة دولة ✅
23. **إشعارات Web Push** (طلب/توصيل مُسنَد) ✅
24. استيراد الطلبات/المنتجات من Excel/CSV مع ربط الأعمدة ✅
25. ثيم فاتح/داكن + عربي/إنجليزي (RTL) ✅
26. اشتراكات SaaS بخطط + مفاتيح ميزات لكل شركة (company_features) ✅
27. أسباب إلغاء/إرجاع قابلة للتخصيص + تحليلها ✅
28. طباعة/نسخ تفاصيل الطلب ✅

---

# 16) تحليل النواقص (بدون حلول)

- ⚠️ **صفحة تسوية الشحن `/shipping/settlement` معطوبة** (React error #310 — شاشة بيضاء). ✅
- ❌ لا وحدة **فواتير رسمية** (Invoices) ولا **ضرائب/VAT**. ✅
- ❌ لا **مرتجعات مالية/استرداد (Refunds)** كوحدة مستقلة (مجرد حالة "مرتجع"). 🟡
- ❌ عدم اتساق العملة: صفحة العملاء تعرض `$0.00` بينما النظام بالجنيه. ✅
- ❌ التنقّل المباشر بروابط المسارات المتداخلة (`/finances/*`, `/customers/{id}`) يعيد للوحة التحكم عند إعادة التحميل (كسر Deep-linking/تحديث الصفحة). ✅
- ❌ لا **سجل تدقيق (Audit Log)** شامل ظاهر على مستوى النظام (فقط سجل نشاط لكل طلب). 🟡
- ❌ لا **بوابة عميل/تتبع للعميل النهائي** ظاهرة. 🟡
- ❌ لا **إدارة صلاحيات دقيقة (Granular Permissions)** خارج حالات الطلبات (الصلاحيات مرتبطة بالحالات فقط). ✅
- ❌ لا **باركود/QR** لمسح المنتجات في المخزون ظاهر. 🟡
- ❌ ترقيم/طيّ مفقود في تنبيهات المخزون بلوحة التحكم (548 عنصراً). ✅
- ❌ نسب "من الأسبوع الماضي" في لوحة التحكم تبدو ثابتة (غير محسوبة). 🟡
- ❌ الدور "محاسب" غير مُعرَّف ضمن دليل حالات الطلبات (غموض في صلاحياته). ✅
- ❌ لا **تكامل POS/متجر إلكتروني** ظاهر بخلاف الاستيراد اليدوي (رغم وجود طلبات برقم WC- يوحي بـ WooCommerce مستورد). 🟡

---

# 17) تحليل المنافسة (أنظمة مشابهة)

النظام يقع في فئة **Order & COD Management للأسواق الناشئة (مصر/الخليج)**، ويشبه:

- **أنظمة مصرية/عربية:** Bosta Fleet/Dash، Wuilt/Zammit، Taager، DالOrderDesk، **قنوات بيع سوشيال + COD** — أقرب المنافسين المحليين.
- **إقليمي/عالمي:** ShipBlu، Ecwid، **EasyOrders**، **Wared/Daftra** (ERP عربي)، Orderhive، Zoho Inventory، **Katana** (مخزون/تصنيع).
- **قنوات السوشيال كومرس:** كأداة تحويل رسائل انستجرام/واتساب إلى طلبات — يشبه منطق **iSell / Bolddesk / ChatBiz**.
- **التميّز:** الدمج بين (إدارة طلبات + تأكيد واتساب + تكامل شركات شحن مصرية متعددة + محاسبة نقدية وإقفال شهري) في منتج واحد موجّه للسوق المصري بالعربية.

---

# 18) الوثيقة النهائية — خريطة النظام الشاملة

## 18.1 خريطة الصفحات (Site Map)
```
/                         لوحة التحكم
/orders                   الطلبات (+ Sheet تفاصيل + Dialog إنشاء)
/orders/bulk-shipment     الشحن بالجملة
/customers                العملاء
/customers/{uuid}         تفاصيل العميل
/team                     الفريق والأدوار
/inventory                المنتجات/المخزون (+ متغيرات)
/warehouses               المستودعات + التحويلات
/expenses                 المصروفات
/analytics                التحليلات (5 تبويبات)
/shipping/settlement      تسوية الشحن ⚠️ معطوبة
/finances                 (يعيد التوجيه)
/finances/suppliers       الموردون
/finances/purchase-orders أوامر الشراء
/finances/close           الإقفال الشهري
/settings                 الإعدادات (7 تبويبات)
```

## 18.2 ملخص الموديولات
Dashboard · Orders · Bulk Shipment · Customers · Team/Roles · Products/Inventory · Warehouses · Expenses · Analytics · Shipping · Finances (Suppliers/PO/Close) · Settings · Notifications · Multi-Company.

## 18.3 ملخص الجداول المتوقعة (26 مؤكد + ~8 مرجّح)
**مؤكدة ✅:** companies, company_members, company_features, profiles, customers, orders, order_items, order_labels, order_label_assignments, order_reasons, whatsapp_confirmations, products, inventories, inventory_stock, stock_transfers, suppliers, purchase_orders, po_items, po_payments, po_receiving_logs, expenses, month_closes, capital_log, notifications, shipping_offices, auth.users.
**مرجّحة 🟡:** shipments, shipping_zones, carrier_connections, shipping_settlements, order_comments, activity_log, subscriptions/plans, stock_transfer_items.

## 18.4 ملخص RPC ✅
get_company_members_with_profiles · get_order_status_counts · get_inventory_products · get_analytics_summary (+ مرجّح: close_month, create_shipment, smart_paste_parse).

## 18.5 ملخص الـ Workflows
دورة الطلب (استقبال→تأكيد→تجهيز→شحن→تسليم→تحصيل→إغلاق) · دورة الشراء والمخزون · الدورة المالية والإقفال · إعداد الشركة · الإسناد وقياس الأداء.

## 18.6 ملخص الصلاحيات
9 أدوار مبنية على رؤية/تغيير حالات الطلبات (كامل الصلاحية: مسؤول/مدير/موظف؛ محدودة: موديريتور/كول سنتر/تجهيز/شحن/توصيل؛ بلا وصول للطلبات: مدير المستودع؛ محاسب = مالي).

## 18.7 ملخص القيم الثابتة (Enums)
- **حالات الطلب (12):** جديد, جاري التأكيد, قيد المعالجة, ناقص, جاهز, تم الشحن, تم التسليم, مكتمل, مؤجل, ملغي, مرتجع, مستبدل.
- **حالة الدفع:** غير مدفوع, مدفوع جزئياً, مدفوع مسبقاً, مدفوع بالكامل.
- **حالة المتابعة:** قيد الانتظار (+ أخرى 🟡).
- **فئات المصروفات:** الرواتب, الصيانة, المواد, الأصول, إعلانات, أخرى.
- **حالة الاستلام (PO):** لم يُستلم, جزئي, مستلم بالكامل.
- **نوع المورد:** محلي (+ مستورد 🟡).
- **نوع العميل:** Regular (+ أنواع أخرى 🟡).

## 18.8 المكدّس التقني (خلاصة)
React + Vite (SPA, code-split) · Radix/shadcn + Tailwind · Supabase (PostgreSQL + PostgREST + Auth/GoTrue + RPC + Edge Functions/Webhooks) · Web Push · متعدد المستأجرين بـ company_id (+RLS مرجّح) · تكاملات شركات شحن متعددة.

---

## ملحق: قائمة النواقص/الأعطال التي تحتاج انتباهاً عند إعادة البناء
1. عطل شاشة تسوية الشحن (Hooks). 2. كسر Deep-linking عند إعادة تحميل المسارات المتداخلة. 3. عدم اتساق العملة ($/ج.م.). 4. غياب الفواتير/الضرائب. 5. غموض صلاحيات دور "محاسب". 6. غياب ترقيم بعض القوائم الكبيرة. 7. نسب لوحة التحكم تبدو ثابتة.

> **انتهت الوثيقة.** جميع التصنيفات (مؤكد/مرجّح/تخمين) موضّحة داخل كل قسم. تمّت تغطية كل صفحة يمكن الوصول إليها بحساب الاختبار المُعطى، مع اعتراض طلبات الشبكة الحقيقية لتأكيد بنية قاعدة البيانات والـ APIs.
