# 02 — ARCHITECTURE
## المعمارية والمكدّس التقني الكامل لنظام OrdersFlow

> **حالة الوثيقة:** مرجع رسمي معتمد (Authoritative Reference)
> **نطاق هذا الملف:** البنية المعمارية، المكدّس التقني، طبقات النظام، تدفق البيانات، نموذج العزل، الأمان، والأداء على المستوى المعماري.
> **النظام:** OrdersFlow — `https://app.ordersflow.site`
> **مشروع Supabase المرصود:** `gvvzqqcukynhapdgvcmj`
> **تصنيف الثقة:** ✅ مؤكد · 🟡 مرجّح · ⚪ تخمين
> **هذا الملف مستقل بذاته** — يُقرأ دون الرجوع لأي ملف آخر.

---

# Overview

OrdersFlow مبني على معمارية **"Backend-as-a-Service" (BaaS) موجّهة للعميل (Client-Centric)**، حيث يتحمّل المتصفح (React SPA) الجزء الأكبر من منطق العرض والتنسيق، بينما تُدار البيانات والأمان ومنطق الأعمال الثقيل عبر **Supabase** (PostgreSQL + PostgREST + Auth + RPC + Edge Functions).

هذه المعمارية تختلف جوهريًا عن النمط التقليدي (Frontend → API Server → Database). لا يوجد خادم تطبيقات وسيط (Application Server) بالمعنى التقليدي؛ بدلًا من ذلك:

```
[React SPA في المتصفح]  ⇄  [PostgREST / RPC / Edge Functions]  ⇄  [PostgreSQL + RLS]
                        ⇄  [Supabase Auth (GoTrue)]
                        →  [خدمة تحليلات خارجية (events)]
                        ←  [Webhooks من شركات الشحن]
                        →  [Web Push للأجهزة]
```

**الدلالة الحرجة (✅):** لأن المتصفح يتحدث مباشرة إلى PostgREST بتوكن المستخدم، فإن **Row Level Security (RLS) في Postgres هو خط الدفاع الأمني الأساسي والوحيد تقريبًا** — لا توجد طبقة API وسيطة تفرض الصلاحيات. أي منتج منافس يتبنى نفس المعمارية يجب أن يعامل RLS كمكوّن حرج من الدرجة الأولى.

---

# Business Purpose (للمعمارية)

اختيار معمارية BaaS/Supabase يخدم أهدافًا تجارية واضحة لمنتج SaaS صغير/متوسط:

| الهدف التجاري | كيف تخدمه المعمارية |
|---------------|---------------------|
| **سرعة التطوير (Time to Market)** | PostgREST يولّد API تلقائيًا من الجداول؛ لا حاجة لكتابة CRUD يدويًا |
| **تكلفة تشغيل منخفضة** | لا خوادم تطبيقات لإدارتها؛ Supabase مُدار |
| **قابلية التوسع الأولية** | Postgres + PostgREST يكفيان لآلاف المستأجرين الصغار |
| **أمان مركزي** | RLS يفرض العزل في مكان واحد (قاعدة البيانات) |
| **مرونة الميزات** | `company_features` (Feature Flags) يربط الخطط بالميزات |

**المقايضة (⚪):** هذه المعمارية ممتازة للبدء السريع لكنها تنقل تعقيد الأمان بالكامل إلى RLS، وتصعّب منطق الأعمال متعدد الخطوات (يُدفع نحو RPC/Edge Functions).

---

# Functional Description (الطبقات المعمارية)

## طبقات النظام (System Layers)

| الطبقة | المسؤولية | التقنية | الثقة |
|-------|-----------|---------|------|
| **1. العرض (Presentation)** | واجهة المستخدم، التوجيه، حالة العرض | React SPA (Vite) + Radix/shadcn + Tailwind | ✅ |
| **2. حالة العميل (Client State)** | تفضيلات، فلاتر، شركة حالية، ثيم | localStorage + حالة React | ✅ |
| **3. الوصول للبيانات (Data Access)** | CRUD تلقائي، فلاتر، ربط الجداول | Supabase JS Client → PostgREST | ✅ |
| **4. منطق الأعمال (Business Logic)** | التجميعات، العمليات المعقّدة | Postgres Functions (RPC) | ✅ |
| **5. التكاملات (Integrations)** | الشحن، Webhooks، معالجة خارجية | Edge Functions + Webhooks | ✅ |
| **6. المصادقة (Auth)** | تسجيل الدخول، الجلسات، JWT | Supabase Auth (GoTrue) | ✅ |
| **7. التخزين (Persistence)** | البيانات + العزل | PostgreSQL + RLS | ✅ (RLS 🟡) |
| **8. الإشعارات (Push)** | إشعارات الأجهزة | Web Push API | ✅ |
| **9. التتبّع (Telemetry)** | أحداث الاستخدام | خدمة خارجية (`mpc-prod-*.run.app/events`) | ✅ |

---

# المكدّس التقني الكامل (Complete Tech Stack)

> جميع البنود أدناه **مؤكدة ✅** من طلبات الشبكة أو localStorage أو أسماء الحزم، ما لم يُذكر خلاف ذلك.

## Frontend

| المكوّن | التقنية | الدليل | الثقة |
|--------|---------|--------|------|
| **إطار العمل** | React (SPA) | بنية المكوّنات، أخطاء React (#310) | ✅ |
| **أداة البناء** | Vite | أسماء الحزم المُقسّمة `ShippingSettlement-*.js`, `BulkShipmentPage-*.js`, `ui-*.js` | ✅ |
| **تقسيم الكود** | Code-splitting لكل صفحة | حزمة منفصلة لكل شاشة | ✅ |
| **مكتبة المكوّنات** | Radix UI / shadcn | خصائص `data-radix-*` على tabs/dialog/combobox | ✅ |
| **التنسيق** | Tailwind CSS | أنماط الفئات المرصودة | ✅ |
| **الأيقونات** | Lucide (🟡) | ظهور `file-down` | 🟡 |
| **الثيم** | فاتح/داكن | مفتاح `theme` في localStorage | ✅ |
| **التوطين** | عربي (RTL)/إنجليزي | مفتاح `app-language`، تخطيط RTL | ✅ |
| **PWA** | مرجّح | Web Push + ضغط الصور محليًا | 🟡 |

## Backend / Data

| المكوّن | التقنية | الدليل | الثقة |
|--------|---------|--------|------|
| **قاعدة البيانات** | PostgreSQL (عبر Supabase) | مشروع `gvvzqqcukynhapdgvcmj` | ✅ |
| **طبقة API** | PostgREST | مسارات `/rest/v1/<table>` | ✅ |
| **منطق معقّد** | Postgres Functions (RPC) | `/rest/v1/rpc/get_analytics_summary` وغيرها | ✅ |
| **المصادقة** | Supabase Auth (GoTrue) | مفتاح `sb-gvvzqqcukynhapdgvcmj-auth-token`، `POST /auth/v1/token` | ✅ |
| **التكاملات** | Edge Functions + Webhooks | سجل "Webhook"/"Create Shipment" في تبويب الشحن | ✅ |
| **العزل** | Row Level Security (RLS) | استنتاج من `company_id=eq.` في كل استعلام | 🟡 |

## External Services

| الخدمة | الغرض | الدليل | الثقة |
|-------|-------|--------|------|
| **شركات الشحن** | Bosta, QPExpress, ShopShip, J&T, Speedaf, Accurate, Mylerz, LogesTechs | قائمة شركات الشحن في الإعدادات | ✅ |
| **Web Push** | إشعارات الأجهزة حتى عند إغلاق التطبيق | نص إعدادات الإشعارات | ✅ |
| **خدمة التتبّع** | أحداث الاستخدام | `POST mpc-prod-*.run.app/events` | ✅ |
| **واتساب** | قوالب رسائل (مرجّح: روابط wa.me لا API رسمي) | قوالب + بادئة دولة | 🟡 |

---

# UI Components (المعمارية الأمامية)

> التفاصيل التصميمية الكاملة في `17_UI.md`. هنا البنية المعمارية للمكوّنات فقط.

- **نمط المكوّنات:** مكوّنات مركّبة (Composable) بنمط shadcn — كل مكوّن Radix مغلّف بطبقة Tailwind.
- **المكوّنات الأساسية المرصودة:** Dialog (النماذج)، Sheet (لوحة تفاصيل الطلب الجانبية)، Tabs (التبويبات)، Combobox (تغيير الحالة/الإسناد)، Checkbox (التحديد الجماعي)، Table، Card.
- **التوجيه (Routing):** موجّه من جانب العميل (Client-side Router) — مرجّح React Router 🟡. دليل ذلك: كسر Deep-linking عند إعادة تحميل المسارات المتداخلة (المسارات تُحلّ داخليًا لا عبر الخادم).
- **تحميل الحزم:** كسول (Lazy) لكل صفحة عبر code-splitting.

---

# نموذج تدفق البيانات (Data Flow Model)

## قراءة نموذجية (Read Flow) ✅

```
1. المستخدم يفتح شاشة (مثل /orders)
2. React يحمّل حزمة الصفحة (lazy)
3. Supabase JS Client يبني استعلام PostgREST:
   GET /rest/v1/orders?select=*,order_items(*),...&company_id=eq.<uuid>&status=eq.<x>&order=created_at.desc&limit=1000
4. التوكن (JWT) يُرفق في الهيدر (Authorization: Bearer)
5. PostgREST يطبّق RLS حسب company_id في التوكن/العضوية
6. النتيجة JSON تعود مباشرة للمتصفح
7. React يعرض البيانات
```

## كتابة نموذجية (Write Flow) ✅

```
1. المستخدم يملأ نموذجًا (مثل إنشاء طلب)
2. Supabase JS Client يرسل POST /rest/v1/orders
3. RLS يتحقق من صلاحية الكتابة (company_id مطابق)
4. Postgres يكتب الصف (+ Triggers محتملة لتحديث مشتقات مثل total_orders) ⚪
5. النتيجة تعود، والواجهة تُحدَّث
```

## عملية معقّدة (RPC Flow) ✅

```
1. عملية تتطلب تجميعًا/منطقًا (مثل عدّادات الحالة)
2. POST /rest/v1/rpc/get_order_status_counts { company_id, filters }
3. دالة Postgres تنفّذ التجميع داخل القاعدة (كفاءة عالية)
4. تعيد نتيجة مُجمّعة واحدة بدل آلاف الصفوف
```

## تكامل الشحن (Integration Flow) ✅/🟡

```
1. المستخدم ينشئ شحنة → استدعاء Edge Function (create_shipment) 🟡
2. Edge Function يتصل بـ API شركة الشحن (Bosta...) بمفاتيح الشركة
3. تُنشأ الشحنة ويُرجَع رقم التتبع + رابط البوليصة
4. لاحقًا: شركة الشحن ترسل Webhook بتحديث الحالة
5. Edge Function يستقبل الـ Webhook ويحدّث حالة الطلب/الشحنة
```

---

# نموذج العزل بين المستأجرين (Tenant Isolation) ✅

- **المبدأ:** Shared Database / Shared Schema — كل المستأجرين في نفس الجداول.
- **آلية العزل:** عمود `company_id (uuid)` في كل جدول أعمال.
- **الفلترة:** كل استعلام PostgREST يحمل `?company_id=eq.<uuid>` ✅.
- **مصدر الشركة الحالية:** `currentCompanyId` في localStorage ✅.
- **العضوية:** `company_members (user_id, company_id, role, status)` تحدد أي شركات يصلها المستخدم ✅.
- **الأمان:** RLS يمنع تجاوز `company_id` حتى لو تلاعب العميل بالاستعلام 🟡.
- **تعدد الشركات للمستخدم:** مستخدم واحد → عضويات متعددة → تبديل فوري بين الشركات ✅.

**نقطة حرجة لإعادة البناء (⚪):** يجب أن تضمن سياسات RLS أن `company_id` في أي عملية = إحدى شركات عضوية المستخدم، وإلا انكشفت بيانات المستأجرين. هذا أخطر جزء معماري.

---

# State Management (إدارة الحالة) ✅

## حالة محفوظة محليًا (localStorage)

| المفتاح | الغرض | الثقة |
|--------|-------|------|
| `theme` | الثيم (فاتح/داكن) | ✅ |
| `currentCompanyId` | الشركة النشطة | ✅ |
| `app-language` | اللغة | ✅ |
| `ordersV2FilterState` | حالة فلاتر الطلبات المحفوظة | ✅ |
| `ordersV2ViewMode` | وضع العرض (جدول/بطاقات) | ✅ |
| `sb-gvvzqqcukynhapdgvcmj-auth-token` | توكن جلسة Supabase | ✅ |

**ملاحظة (🟡):** بادئة `ordersV2` تدل على أن وحدة الطلبات أُعيد بناؤها (إصدار ثانٍ) — مؤشر على تعقيدها وأهميتها. حالة العرض والفلاتر تُحفظ محليًا لتحسين تجربة العودة للصفحة.

## حالة الخادم (Server State)

- تُدار عبر Supabase JS Client؛ مرجّح استخدام مكتبة جلب بيانات (React Query أو ما يشابهها) 🟡 — بدليل جلب البيانات المتوازي عند فتح الصفحة (6–10 طلبات).

---

# Performance Considerations (على المستوى المعماري)

> التفاصيل لكل شاشة في ملفاتها. هنا المعماري.

**إيجابيات معمارية ✅:**
- **RPC للتجميعات الثقيلة:** عدّادات الحالة والتحليلات تُحسب في القاعدة لا على العميل.
- **Code-splitting:** كل صفحة حزمة منفصلة → تحميل أولي أسرع.
- **ضغط الصور محليًا:** قبل الرفع → تقليل حجم النقل.

**نقاط ضعط محتملة (✅ مرصودة):**
- `products?...&limit=10000` — تحميل حتى 10000 منتج دفعة واحدة (شاشة المستودعات).
- `orders?...&limit=1000` — تحميل حتى 1000 طلب للفترة (بلا ترقيم خادمي حقيقي).
- لوحة التحكم تجلب كل تنبيهات المخزون (548) بلا ترقيم.
- **6–10 طلبات REST متوازية** عند فتح صفحة (بيانات + عضوية + إشعارات + ميزات + شركة).

**بيانات شبه ثابتة تُجلب مع كل تنقّل (تحتاج cache) 🟡:**
`company_features`, `order_labels`, `order_reasons`, `inventories`, قائمة المحافظات.

---

# Security Considerations (معماري)

| الجانب | التفصيل | الثقة |
|-------|---------|------|
| **المصادقة** | JWT من GoTrue، يُرفق في كل طلب | ✅ |
| **التفويض** | RLS في Postgres (خط الدفاع الأساسي) | 🟡 |
| **كشف API مباشر** | PostgREST مكشوف للعميل → RLS إلزامي على كل جدول | ✅ |
| **العمليات الحساسة** | حذف الحساب/تغيير كلمة المرور عبر GoTrue | ✅ |
| **مفاتيح شركات الشحن** | مرجّح تُخزّن في القاعدة وتُستخدم من Edge Functions لا من العميل | 🟡 |
| **التوكن في localStorage** | عرضة لـ XSS نظريًا (نمط Supabase الافتراضي) | ⚪ |

**مخاطر معمارية لإعادة البناء (⚪):**
1. أي جدول بلا RLS = تسريب بيانات كل المستأجرين.
2. منطق الأعمال في العميل يمكن تجاوزه → القواعد الحرجة يجب أن تُفرض في القاعدة (RLS/Triggers/RPC).
3. حدود الاستعلام العالية (10000) قد تُستغل لاستنزاف الموارد.

---

# Deployment & Infrastructure (مستنتَج) 🟡

| البند | الاستنتاج | الثقة |
|------|-----------|------|
| **استضافة Frontend** | مرجّح CDN/Static hosting للـ SPA (Vercel/Netlify/Cloudflare نمطيًا) | ⚪ |
| **استضافة Backend** | Supabase Cloud (مُدار) | ✅ |
| **Edge Functions** | Supabase Edge (Deno) — نمطيًا | 🟡 |
| **خدمة التتبّع** | Google Cloud Run (`*.run.app`) | ✅ (من الدومين) |
| **الإشعارات** | Web Push (VAPID) عبر Service Worker | 🟡 |

---

# Edge Cases & Failure Scenarios (معماري)

| السيناريو | الأثر | الثقة |
|----------|------|------|
| **خطأ ترتيب Hooks** | شاشة تسوية الشحن تتعطل كليًا (React #310) — مكوّن واحد يعطّل صفحة كاملة | ✅ |
| **إعادة تحميل مسار متداخل** | التوجيه من جانب العميل يفشل → إعادة للوحة التحكم (لا fallback خادمي) | ✅ |
| **فشل RLS** | (نظري) كشف بيانات عبر المستأجرين | ⚪ |
| **انقطاع Webhook الشحن** | حالات الشحنات لا تُحدَّث تلقائيًا | 🟡 |
| **تجاوز حد الاستعلام** | صفحات بمخزون/طلبات ضخمة قد تبطئ أو تفشل | 🟡 |

---

# Logging & Audit Requirements (معماري)

> التفصيل الوظيفي في الملفات المعنية. المرصود معماريًا:

- **تتبّع الأحداث:** أحداث الاستخدام تُرسل لخدمة خارجية (`/events`) ✅.
- **سجل نشاط الطلب:** موجود على مستوى الطلب (تبويب "سجل النشاط") ✅.
- **سجل نشاط الشحن:** Webhook/Create Shipment مسجّل في تبويب الشحن ✅.
- **غياب Audit Log مركزي:** لا سجل تدقيق شامل على مستوى النظام مرصود 🟡.

---

# Technical Notes

- **PostgREST** يحوّل الجداول إلى REST API تلقائيًا مع دعم فلاتر غنية (`eq/in/gte/lte/not.in/order/limit/offset`) وربط الجداول المضمّن (`select=*,order_items(*)`). هذا يفسّر بنية الاستعلامات المرصودة.
- **RPC** يُستخدم حصريًا لما لا يمكن التعبير عنه بـ CRUD بسيط: التجميعات، العمليات متعددة الجداول، منطق الأعمال الذري.
- **Edge Functions** ضرورية للتكاملات الخارجية (شركات الشحن) لأنها تحتاج مفاتيح سرية لا يجوز كشفها للعميل.
- **تسمية `inventories`** للمستودعات (لا `warehouses`) خيار تسمية داخلي مهم للانتباه له في القاعدة.

---

# Reverse Engineering Notes

- المكدّس التقني مؤكد بدرجة عالية من ثلاثة مصادر مستقلة: أسماء الحزم (Vite)، مفاتيح localStorage (Supabase)، ومسارات الشبكة (PostgREST/RPC/Auth).
- معرّف مشروع Supabase (`gvvzqqcukynhapdgvcmj`) مرصود مباشرة من مفتاح التوكن ومسارات REST.
- وجود RLS **مرجّح 🟡 لا مؤكد** — لأننا لا نرى سياسات القاعدة، لكن الاعتماد على PostgREST المباشر يجعل RLS ضرورة معمارية شبه مؤكدة.
- Edge Functions مؤكدة من سجل نشاط الشحن، لكن أسماؤها الدقيقة وتفاصيلها مرجّحة.
- البنية التحتية للنشر (Frontend hosting) غير مرصودة مباشرة — تخمين معماري ⚪.

---

# Confidence Level

| الجانب | الثقة |
|-------|------|
| Frontend = React + Vite + Radix/shadcn + Tailwind | ✅ مؤكد |
| Backend = Supabase (Postgres + PostgREST + Auth + RPC) | ✅ مؤكد |
| معرّف مشروع Supabase | ✅ مؤكد |
| Edge Functions + Webhooks للشحن | ✅ مؤكد (الوجود) / 🟡 (التفاصيل) |
| RLS كآلية عزل | 🟡 مرجّح |
| نمط الحالة (React Query) | 🟡 مرجّح |
| البنية التحتية للنشر | ⚪ تخمين |
| PWA | 🟡 مرجّح |

---

READY FOR NEXT FILE
