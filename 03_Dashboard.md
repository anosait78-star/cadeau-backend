# 03 — DASHBOARD
## التوثيق الهندسي الكامل لصفحة لوحة التحكم (`/`)

> **حالة الوثيقة:** مرجع رسمي معتمد (Authoritative Reference)
> **نطاق هذا الملف:** تحليل صفحة لوحة التحكم عنصرًا عنصرًا — المكوّنات، البيانات، الـ APIs، الأداء، الحالات، والقواعد.
> **المسار:** `/` (الصفحة الرئيسية بعد تسجيل الدخول)
> **الشركة المرجعية:** Cadeau Egypt
> **تصنيف الثقة:** ✅ مؤكد · 🟡 مرجّح · ⚪ تخمين
> **هذا الملف مستقل بذاته** — يُقرأ دون الرجوع لأي ملف آخر.

---

# Overview

لوحة التحكم (Dashboard) هي **الشاشة الافتتاحية** التي يراها المستخدم فور تسجيل الدخول (المسار الجذر `/`). وظيفتها تقديم **نظرة عامة سريعة (At-a-glance)** على صحة العمل: مؤشرات الأداء الرئيسية، توزيع الطلبات على الحالات، آخر النشاط، وتنبيهات المخزون الحرجة.

الصفحة **للقراءة والتنقّل فقط (Read-only / Navigational)** — لا تحتوي على نماذج إدخال أو عمليات كتابة. كل عنصر فيها هو إما مؤشر عرض أو نقطة انطلاق (Entry Point) للتنقّل إلى الشاشة التفصيلية المعنية (طلب، منتج، حالة).

**التصنيف الوظيفي:** شاشة تجميعية (Aggregation Dashboard) تعتمد بالكامل على دوال RPC للتجميع وعلى استعلامات قراءة مُفلترة.

---

# Business Purpose

| الغرض | التفصيل |
|------|---------|
| **نقطة انطلاق يومية** | أول ما يفتحه صاحب العمل/المدير صباحًا لتقييم الوضع | 
| **رصد الصحة العامة** | مؤشرات مالية وتشغيلية في مكان واحد (طلبات، دخل، مخزون، فريق) |
| **إبراز ما يحتاج تدخّلًا** | تنبيهات المخزون المنخفض + توزيع الحالات (طلبات عالقة) |
| **تقليل زمن الوصول للمعلومة** | بدل تصفّح كل شاشة، ملخص مركّز |

**الجمهور الأساسي (🟡):** الأدوار كاملة الصلاحية (مسؤول/مدير/موظف) التي ترى كل الحالات. الأدوار المحدودة (توصيل/مستودع) قد ترى نسخة مقيّدة أو لا تصل للوحة أصلًا 🟡.

---

# Functional Description

الصفحة تعرض أربع كتل معلوماتية رئيسية، مرتّبة من الأعلى (الأهم/الأعم) إلى الأسفل (التفصيلي):

1. **بطاقات KPI (4 بطاقات)** — المؤشرات العليا.
2. **الطلبات حسب الحالة** — شبكة عدّادات لكل الحالات الـ12.
3. **الطلبات الأخيرة** — آخر 5 طلبات.
4. **تنبيهات المخزون المنخفض** — قائمة المنتجات تحت الحد الأدنى.

كل كتلة تُغذّى ببيانات مستقلة (استعلام/RPC منفصل)، ما يفسّر تعدد طلبات الشبكة عند فتح الصفحة.

---

# Complete Screen Breakdown

تفكيك كامل للصفحة من الأعلى للأسفل:

### الكتلة 1: بطاقات KPI (4) ✅

| البطاقة | القيمة المرجعية | مؤشر التغيّر | المصدر المرجّح |
|--------|-----------------|--------------|-----------------|
| **إجمالي الطلبات** | 203 | +12% (أسبوعي) | عدّ `orders` |
| **صافي الدخل** | 57,467 ج.م. | +8% (أسبوعي) | RPC تحليلي (محصّل − مصروفات − COGS) |
| **المخزون** | 564 | — | عدّ `products` أو مجموع `inventory_stock` |
| **أعضاء الفريق** | 9 | — | عدّ `company_members` |

**ملاحظة حرجة (🟡):** نسب التغيّر الأسبوعية (+12% / +8%) **تبدو ثابتة/تجريبية (Placeholder)** — لم يُرصد ما يؤكد أنها محسوبة فعليًا من بيانات الأسبوع السابق. يجب معاملتها كـ **غير موثوقة** عند إعادة البناء.

### الكتلة 2: الطلبات حسب الحالة ✅

- شبكة عدّادات، عدّاد لكل حالة من الحالات الـ12: جديد، جاري التأكيد، قيد المعالجة، ناقص، جاهز، تم الشحن، تم التسليم، مكتمل، مؤجل، ملغي، مرتجع، مستبدل.
- كل عدّاد يعرض العدد الحالي للطلبات في تلك الحالة.
- **مصدر البيانات:** RPC `get_order_status_counts` ✅ (نفس الدالة المستخدمة في تبويبات صفحة الطلبات).
- **سلوك التنقّل المرجّح (🟡):** النقر على حالة ينقل إلى `/orders` مع تفعيل تبويب تلك الحالة.

### الكتلة 3: الطلبات الأخيرة (آخر 5) ✅

- قائمة بآخر 5 طلبات، كل صف يعرض: **اسم العميل، التاريخ، القيمة (الإجمالي)، الحالة**.
- **مصدر البيانات:** `GET /rest/v1/orders?...&order=created_at.desc&limit=5` 🟡.
- **التنقّل:** النقر على طلب يفتح تفاصيله (مرجّح: ينقل إلى `/orders` ويفتح لوحة التفاصيل الجانبية) 🟡.

### الكتلة 4: تنبيهات المخزون المنخفض ✅

- قائمة **مطوّلة (548 عنصرًا في Cadeau Egypt)** لكل منتج نفد أو اقترب من الحد الأدنى.
- كل صف يعرض: **اسم المنتج، المستودع، الكمية المتبقية، الحد الأدنى**.
- **مصدر البيانات:** استعلام على `inventory_stock` مع مقارنة `on_hand ≤ low_stock_threshold` 🟡 (مرجّح عبر RPC أو استعلام مُركّب).
- **عطل UX مرصود (✅):** القائمة **بلا ترقيم ولا طيّ** — 548 عنصرًا تُعرض دفعة واحدة، مشكلة أداء وتجربة موثّقة في `19_Missing_Features.md`.

---

# UI Components

> نظام التصميم الكامل في `17_UI.md`. المكوّنات المستخدمة هنا:

| المكوّن | الاستخدام | الثقة |
|--------|-----------|------|
| **Card** | بطاقات KPI الأربع + حاويات الكتل | ✅ |
| **Stat/Metric** | عرض القيمة + التسمية + مؤشر التغيّر | ✅ |
| **Badge/Counter** | عدّادات الحالات | ✅ |
| **List/Table (مبسّط)** | الطلبات الأخيرة + تنبيهات المخزون | ✅ |
| **Status Badge ملوّن** | حالة الطلب في القوائم | ✅ |
| **Icons (Lucide 🟡)** | أيقونات البطاقات | 🟡 |

---

# UX Analysis

**نقاط القوة ✅:**
- تدرّج معلوماتي منطقي (الأعم فالأخص).
- تجميع المؤشرات الحرجة في شاشة واحدة يقلّل زمن اتخاذ القرار.
- عدّادات الحالات تعطي صورة فورية عن "أين عالقة الطلبات".

**نقاط الاحتكاك ✅/🟡:**
- **قائمة تنبيهات المخزون طويلة جدًا (548) بلا ترقيم/طيّ** — تُغرق الصفحة وتضر الأداء. ✅
- **نسب التغيّر تبدو ثابتة** — تعطي إحساسًا زائفًا بالدقة. 🟡
- **لا فلاتر زمنية على مستوى اللوحة** — المؤشرات لفترة ثابتة/إجمالية غير واضحة الحدود. 🟡
- **كثافة معلوماتية عالية** أسفل الصفحة (المخزون) مقابل خفّة أعلاها. ✅

---

# Navigation

- **الوصول للصفحة:** المسار الجذر `/` — تفتح افتراضيًا بعد الدخول، ومن الشريط الجانبي (أيقونة الرئيسية).
- **التنقّل الخارج منها (Outbound):**
  - عدّاد حالة → `/orders` (تبويب الحالة) 🟡.
  - طلب أخير → تفاصيل الطلب 🟡.
  - تنبيه مخزون → تفاصيل المنتج / `/inventory` 🟡.
- **سلوك إعادة التوجيه (✅):** المسارات المتداخلة تعيد للوحة التحكم عند إعادة التحميل (كسر Deep-linking) — اللوحة هي "الملاذ الافتراضي" للتوجيه الفاشل.

---

# User Actions

| الإجراء | النوع | الثقة |
|--------|------|------|
| عرض المؤشرات | قراءة فقط | ✅ |
| النقر للتنقّل (حالة/طلب/منتج) | تنقّل | 🟡 |
| — لا إنشاء/تعديل/حذف على هذه الصفحة | — | ✅ |

**ملاحظة:** الصفحة لا تحتوي أي عملية كتابة (No mutations).

---

# Buttons

- لا توجد أزرار إجراء أساسية (إنشاء/حفظ) على لوحة التحكم. ✅
- العناصر التفاعلية الوحيدة هي **روابط تنقّل** (بطاقات/صفوف قابلة للنقر) 🟡.

---

# Forms

- **لا توجد نماذج** على هذه الصفحة. ✅

---

# Tables

القوائم المعروضة شبه-جدولية (List-style) لا جداول كاملة بأدوات فرز/تحديد:

| القائمة | الأعمدة | الترتيب | ترقيم |
|--------|---------|--------|-------|
| الطلبات الأخيرة | العميل · التاريخ · القيمة · الحالة | `created_at` تنازلي 🟡 | لا (ثابت 5) |
| تنبيهات المخزون | المنتج · المستودع · المتبقي · الحد الأدنى | حسب الأولوية/الاسم 🟡 | ❌ لا ترقيم (عطل) |

---

# Cards

- **بطاقات KPI (4):** موحّدة التصميم — قيمة كبيرة + تسمية + (اختياري) مؤشر تغيّر ملوّن + أيقونة.
- **بطاقات حاوية للكتل:** كل كتلة (الحالات/الأخيرة/المخزون) داخل بطاقة بعنوان.

---

# Charts

- **لا رسوم بيانية** على لوحة التحكم في النسخة المرصودة. ✅
- عدّادات الحالات تُعرض كـ **شبكة أرقام** لا كرسم بياني.
- (الرسوم البيانية الكاملة موجودة في `/analytics` — انظر `09_Analytics.md`).

> **استنتاج (🟡):** غياب الرسوم البيانية عن لوحة التحكم قرار تصميمي (فصل النظرة السريعة عن التحليل العميق)، وقد يمثّل فرصة تحسين (موثّقة في `21`).

---

# Filters

- **لا فلاتر** على لوحة التحكم. المؤشرات إجمالية/لفترة ثابتة غير معلنة. 🟡
- (هذا أحد نقاط الاحتكاك — غياب فلتر زمني للوحة).

---

# Search

- **لا بحث** على لوحة التحكم. ✅

---

# Sorting

- الطلبات الأخيرة مرتّبة زمنيًا تنازليًا (ثابت، غير قابل للتغيير من المستخدم) 🟡.
- تنبيهات المخزون: ترتيب غير مؤكد (مرجّح حسب حدّة النقص أو الاسم) 🟡.

---

# Empty States

حالات فارغة متوقّعة (لشركة جديدة) — مرجّحة 🟡 (لم تُرصد مباشرة لوجود بيانات):

| الكتلة | الحالة الفارغة المتوقّعة |
|-------|--------------------------|
| بطاقات KPI | أصفار (0 طلبات، 0 دخل...) |
| الطلبات حسب الحالة | كل العدّادات = 0 |
| الطلبات الأخيرة | "لا توجد طلبات بعد" + دعوة لإنشاء أول طلب 🟡 |
| تنبيهات المخزون | "لا تنبيهات — كل المخزون فوق الحد الأدنى" 🟡 |

---

# Loading States

- عند فتح الصفحة، كل كتلة تُحمّل بيانتها بشكل مستقل (استعلام منفصل).
- **الحالة المرجّحة (🟡):** هياكل تحميل (Skeletons) أو مؤشرات دوران لكل بطاقة/كتلة أثناء الجلب.
- بسبب تعدد الطلبات المتوازية، قد تظهر الكتل بالتتابع لا دفعة واحدة 🟡.

---

# Error States

- **فشل جلب كتلة:** مرجّح عرض رسالة خطأ داخل الكتلة المعنية دون تعطيل باقي الصفحة 🟡.
- **لا عطل معروف خاص بلوحة التحكم** (بخلاف بطء تحميل قائمة المخزون الكبيرة) ✅.

---

# Validation Rules

- **لا تحقّق إدخال** (الصفحة للقراءة فقط). ✅

---

# Permissions

> التفصيل الكامل في `15_Permissions.md`.

- **الوصول للوحة:** مرجّح لكل الأدوار كاملة الصلاحية (مسؤول/مدير/موظف) ✅.
- **الأدوار المحدودة (🟡):** قد ترى نسخة مقيّدة من العدّادات (فقط الحالات المسموح لها برؤيتها)، أو تُوجَّه لشاشتها الأساسية بدل اللوحة.
- **مدير المستودع:** بلا وصول للطلبات → مرجّح لا يرى كتل الطلبات، وقد يرى المخزون فقط 🟡.

---

# Business Rules

1. **صافي الدخل** المعروض = (المحصّل − المصروفات − تكلفة البضائع المباعة) — نفس تعريف الأناليتكس. ✅ (التعريف) / 🟡 (تطابق قيمة اللوحة).
2. **عدّادات الحالات** تعكس الحالة اللحظية لكل الطلبات ضمن `company_id`. ✅
3. **تنبيه المخزون** يُطلق عندما `on_hand ≤ low_stock_threshold` (أو نفاد كامل) لكل زوج (منتج × مستودع). 🟡
4. **الطلبات الأخيرة** = آخر 5 حسب `created_at`. 🟡

---

# Workflow

**دور اللوحة في سير العمل اليومي:**
```
تسجيل الدخول → لوحة التحكم (نظرة عامة)
   ├── رصد حالات عالقة → النقر → /orders (معالجة)
   ├── رصد نفاد مخزون → النقر → /inventory أو أمر شراء
   └── متابعة صافي الدخل → /analytics (تحليل أعمق)
```
اللوحة **نقطة توجيه (Router)** لا شاشة عمل — تُوجّه المستخدم لأين يتدخّل.

---

# Internal Logic

- كل كتلة = مكوّن React مستقل بجلب بيانات خاص.
- التجميعات الثقيلة (عدّادات الحالة، صافي الدخل) عبر RPC لتجنّب سحب آلاف الصفوف للعميل. ✅
- المخزون المنخفض: مرجّح استعلام مُركّب أو RPC يقارن المخزون بالحد الأدنى عبر كل المستودعات. 🟡

---

# State Changes

- **لا تغييرات حالة** تنشأ من هذه الصفحة (لا كتابة). ✅
- الصفحة تعكس حالة أنشأتها شاشات أخرى.

---

# Possible Database Tables

> المخطط الكامل في `11_Database.md`. الجداول التي تقرأ منها اللوحة:

| الجدول | الاستخدام في اللوحة | الثقة |
|-------|----------------------|------|
| `orders` | إجمالي الطلبات، عدّادات الحالة، الطلبات الأخيرة | ✅ |
| `order_items` | (مرجّح) عدد المنتجات في الطلبات الأخيرة | 🟡 |
| `products` | عدّاد المخزون (عدد المنتجات) | 🟡 |
| `inventory_stock` | تنبيهات المخزون المنخفض (`on_hand` vs `low_stock_threshold`) | ✅ |
| `inventories` | اسم المستودع في التنبيهات | ✅ |
| `company_members` | عدّاد أعضاء الفريق | ✅ |
| `expenses` | (مرجّح) حساب صافي الدخل | 🟡 |

---

# Possible Database Fields

- من `orders`: `id`, `company_id`, `status`, `total`, `collected_amount`, `customer_name`, `created_at`.
- من `inventory_stock`: `product_id`, `inventory_id`, `on_hand`, `low_stock_threshold` (أو من `products`).
- من `products`: `name`, `low_stock_threshold`.
- من `inventories`: `name`.
- من `company_members`: عدّ الصفوف حسب `company_id`.

---

# Relationships

- `orders.company_id → companies.id` (فلترة المستأجر).
- `inventory_stock.product_id → products.id` + `inventory_stock.inventory_id → inventories.id` (تنبيهات المخزون).
- `company_members.company_id → companies.id` (عدّ الفريق).

---

# Possible APIs

> التفصيل الكامل في `13_APIs.md`. الطلبات المتوقّعة عند فتح اللوحة:

| الغرض | الطلب | الثقة |
|------|-------|------|
| عدّادات الحالة | `POST /rest/v1/rpc/get_order_status_counts` | ✅ |
| صافي الدخل / KPIs | `POST /rest/v1/rpc/get_analytics_summary` (مرجّح لجزء منها) | 🟡 |
| الطلبات الأخيرة | `GET /rest/v1/orders?...&order=created_at.desc&limit=5` | 🟡 |
| تنبيهات المخزون | `GET /rest/v1/inventory_stock?...` أو RPC | 🟡 |
| عدّاد الفريق | `GET /rest/v1/company_members?...` أو ضمن سياق مشترك | 🟡 |
| بيانات السياق | company + features + notifications (مشتركة مع كل الصفحات) | ✅ |

---

# Request Flow

```
فتح / 
  → جلب سياق الشركة (company, company_features, company_members) [مشترك]
  → RPC get_order_status_counts (عدّادات الحالة)
  → RPC/تجميع صافي الدخل + KPIs
  → GET orders (آخر 5)
  → GET inventory_stock (تنبيهات المخزون — قد يكون ثقيلًا: 548 صفًا)
كل ذلك متوازٍ → تُعرض الكتل عند وصول بياناتها
```

---

# Response Flow

- كل استجابة JSON تملأ كتلتها المستقلة.
- عدّادات الحالة تعود كخريطة (حالة → عدد).
- تنبيهات المخزون تعود كقائمة صفوف (قد تكون كبيرة — نقطة الأداء).

---

# Performance Considerations

**نقاط الضغط ✅:**
- **تنبيهات المخزون (548 صفًا) بلا ترقيم** — أثقل استعلام في الصفحة، يُحمّل بالكامل.
- **تعدد الطلبات المتوازية** (5–8 طلبات) عند الفتح.

**التوصيات (تُفصّل في `21`، تُذكر هنا كملاحظة أداء):**
- ترقيم/طيّ لقائمة المخزون.
- تخزين مؤقت (cache) لبيانات السياق شبه الثابتة.
- تحميل كسول لكتلة المخزون (أسفل الصفحة) بعد الكتل العليا.

**إيجابيات ✅:** استخدام RPC للتجميعات بدل الحساب على العميل.

---

# Security Considerations

- كل استعلامات اللوحة مُفلترة بـ `company_id` وتخضع لـ RLS 🟡.
- لا عمليات حساسة (قراءة فقط) → سطح هجوم منخفض.
- **نقطة انتباه (🟡):** الأدوار المحدودة يجب ألا ترى في العدّادات حالات لا تملك صلاحية رؤيتها — يتطلب تطبيق فلترة الأدوار على مستوى RPC/RLS لا على العميل فقط.

---

# Edge Cases

| الحالة | السلوك المتوقّع | الثقة |
|-------|------------------|------|
| شركة بلا طلبات | كل العدّادات = 0، حالة فارغة | 🟡 |
| مخزون سليم تمامًا | قائمة تنبيهات فارغة | 🟡 |
| عدد تنبيهات ضخم (آلاف) | بطء تحميل ملحوظ (لا ترقيم) | ✅ |
| دور محدود الصلاحية | عدّادات جزئية أو وصول مقيّد | 🟡 |
| فشل RPC واحد | كتلة واحدة تفشل، الباقي يعمل | 🟡 |

---

# Failure Scenarios

- **فشل `get_order_status_counts`:** كتلة الحالات فارغة/خطأ، باقي اللوحة تعمل 🟡.
- **بطء استعلام المخزون:** الصفحة تبدو "معلّقة" أسفلها حتى يكتمل التحميل ✅.
- **توكن منتهٍ:** إعادة توجيه لتسجيل الدخول (سلوك Auth عام) 🟡.

---

# Logging Requirements

- حدث فتح اللوحة مرجّح ضمن التتبّع العام (`/events`) 🟡.
- لا سجل تدقيق خاص (قراءة فقط) ✅.

---

# Audit Requirements

- **لا متطلبات تدقيق** خاصة باللوحة (لا تغييرات بيانات). ✅

---

# Analytics Events

أحداث تتبّع مرجّحة 🟡:
- `dashboard_viewed`
- `dashboard_kpi_clicked` (أي بطاقة)
- `dashboard_status_counter_clicked` (أي حالة)
- `dashboard_recent_order_clicked`
- `dashboard_low_stock_item_clicked`

---

# Technical Notes

- اللوحة تشترك مع كل الصفحات في جلب **سياق الشركة** (company/features/members/notifications) — لذا جزء من طلباتها ليس خاصًا بها.
- الاعتماد على RPC هو النمط الصحيح للتجميعات؛ نقطة الضعف الوحيدة هي **استعلام المخزون غير المُرقّم**.
- نسب التغيّر الثابتة (Placeholder) دليل على أن اللوحة قد تكون بُنيت بسرعة كـ MVP ولم تكتمل حساباتها.

---

# Reverse Engineering Notes

- القيم (203/57,467/564/9/548) مرصودة مباشرة ✅ من حساب Cadeau Egypt لحظة التحليل.
- عدّادات الحالة مؤكدة الارتباط بـ `get_order_status_counts` (نفس RPC صفحة الطلبات) ✅.
- مصادر باقي الكتل (الطلبات الأخيرة، المخزون، صافي الدخل) **مرجّحة 🟡** — لم تُلتقط أسماء استعلاماتها الدقيقة، لكنها مستنتجة منطقيًا من البيانات المعروضة.
- نسب التغيّر: تصنيفها كـ Placeholder **استنتاج 🟡** مبني على غياب دليل حساب، لا على تأكيد.

---

# Confidence Level

| العنصر | الثقة |
|-------|------|
| بنية اللوحة (4 كتل) وكونها قراءة فقط | ✅ مؤكد |
| قيم KPI الأربع | ✅ مؤكد (لحظة التحليل) |
| عدّادات الحالة عبر get_order_status_counts | ✅ مؤكد |
| تنبيهات المخزون (المحتوى والعطل) | ✅ مؤكد |
| نسب التغيّر كـ Placeholder | 🟡 مرجّح |
| مصادر البيانات الدقيقة لكل كتلة | 🟡 مرجّح |
| سلوك التنقّل الخارج | 🟡 مرجّح |
| الحالات الفارغة/التحميل/الخطأ | 🟡 مرجّح |
| أحداث التتبّع | 🟡 مرجّح |

---

READY FOR NEXT FILE
