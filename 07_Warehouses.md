# 07 — WAREHOUSES
## التوثيق الهندسي الكامل لوحدة المستودعات وتحويلات المخزون (`/warehouses`)

> **حالة الوثيقة:** مرجع رسمي معتمد (Authoritative Reference)
> **نطاق هذا الملف:** تحليل وحدة المستودعات عنصرًا عنصرًا — المستودعات النشطة/المؤرشفة، تحويلات المخزون، البيانات، الـ APIs، والقواعد.
> **المسار:** `/warehouses`
> **الشركة المرجعية:** Cadeau Egypt (3 مستودعات نشطة)
> **تصنيف الثقة:** ✅ مؤكد · 🟡 مرجّح · ⚪ تخمين
> **هذا الملف مستقل بذاته** — يُقرأ دون الرجوع لأي ملف آخر.
> **ملاحظة تسمية حرجة:** المستودع يُخزَّن في جدول اسمه **`inventories`** (لا `warehouses`)، والمخزون الفعلي لكل (منتج × مستودع) في **`inventory_stock`**. تفاصيل المنتجات ومخزونها في `06_Inventory.md`؛ هذا الملف يركّز على **إدارة المواقع والتحويلات بينها**.

---

# Overview

وحدة المستودعات (Warehouses) تدير **مواقع التخزين الفيزيائية** للشركة والتحويلات بينها. في Cadeau Egypt توجد **3 مستودعات نشطة** بتوزيع مخزون متفاوت:
- **المستودع الرئيسي:** 561 منتجًا (المخزون الأساسي).
- **مقر الشركة:** 3 منتجات.
- **مصنع سيد:** 0 منتج.

الوحدة مكمّلة لوحدة المنتجات: المنتجات تُعرَّف مرة واحدة، لكن مخزونها موزّع عبر المستودعات. الوحدة تتيح إضافة مستودعات، تحويل المخزون بينها، تعيين الافتراضي، والأرشفة، مع سجل كامل للتحويلات.

**التصنيف الوظيفي:** إدارة مواقع + عمليات تحويل كمّي بين المواقع.

---

# Business Purpose

| الغرض | التفصيل |
|------|---------|
| **تنظيم التخزين متعدد المواقع** | فصل المخزون حسب الموقع (رئيسي/مقر/مصنع) |
| **دقة التجهيز** | معرفة أين يتوفر الصنف قبل الشحن (كل طلب من مستودع محدد) |
| **حركة المخزون الداخلية** | نقل الكميات بين المواقع حسب الحاجة التشغيلية |
| **الأرشفة دون فقدان** | إيقاف مستودع دون حذف بياناته التاريخية |
| **المركز الافتراضي** | توجيه الطلبات/الاستلام تلقائيًا للمستودع الرئيسي |

**الجمهور:** الأدوار كاملة الصلاحية + **مدير المستودع** (مجاله الأساسي) ✅.

---

# Functional Description

الوحدة مقسّمة إلى **3 تبويبات**:
1. **المستودعات النشطة** — قائمة المواقع العاملة.
2. **المستودعات المؤرشفة** — المواقع الموقوفة.
3. **سجل التحويلات** — تاريخ نقل المخزون بين المستودعات.

مع عمليات: إضافة مستودع، تحويل مخزون، تعيين افتراضي، أرشفة، تعديل.

---

# Complete Screen Breakdown

## التبويب 1: المستودعات النشطة ✅

بيانات كل مستودع:
| البيان | المحتوى | الثقة |
|-------|---------|------|
| **الاسم** | اسم المستودع | ✅ |
| **العنوان** | موقع المستودع | 🟡 |
| **عدد المنتجات** | كم منتجًا له مخزون هنا (رئيسي 561، مقر 3، مصنع 0) | ✅ |
| **الافتراضي** | نجمة/علامة للمستودع الافتراضي | ✅ |
| **الإجراءات** | تعيين افتراضي / تحويل / أرشفة / تعديل | ✅/🟡 |

## التبويب 2: المستودعات المؤرشفة ✅

- المستودعات الموقوفة (`is_archived = true`) — تبقى بياناتها التاريخية.
- مرجّح إمكانية إعادة التنشيط 🟡.

## التبويب 3: سجل التحويلات ✅

سجل كل عمليات النقل:
| العمود | المحتوى | الثقة |
|-------|---------|------|
| **من مستودع** | `from_inventory_id` | ✅ |
| **إلى مستودع** | `to_inventory_id` | ✅ |
| **التاريخ** | `created_at` | ✅ |
| **البنود/الكميات** | المنتجات المحوّلة وكمياتها (`stock_transfer_items` مرجّح) | 🟡 |

## عملية "تحويل المخزون" ✅

- اختيار المستودع المصدر والهدف.
- اختيار المنتج/المنتجات والكميات.
- تنفيذ → خصم من المصدر (`on_hand`−) + زيادة الهدف (`on_hand`+) + تسجيل في `stock_transfers`.

---

# UI Components

| المكوّن | الاستخدام | الثقة |
|--------|-----------|------|
| **Tabs** | النشطة / المؤرشفة / سجل التحويلات | ✅ |
| **Table/List** | قائمة المستودعات + سجل التحويلات | ✅ |
| **Dialog** | إضافة مستودع + تحويل مخزون + تعديل | ✅ |
| **Select** | اختيار المصدر/الهدف/المنتج في التحويل | ✅ |
| **Badge** | علامة الافتراضي / المؤرشف | ✅ |

---

# UX Analysis

**نقاط القوة ✅:**
- **فصل النشطة/المؤرشفة** — واجهة نظيفة دون فقدان التاريخ.
- **سجل تحويلات منفصل** — شفافية حركة المخزون.
- **المستودع الافتراضي** — يقلّل الاختيار المتكرر في الطلبات/الاستلام.

**نقاط الاحتكاك 🟡:**
- **عدد المنتجات فقط** يظهر لكل مستودع؛ رؤية قيمة المخزون لكل مستودع تحتاج `/analytics`.
- **مصنع سيد بـ 0 منتج** — قد يشير لمستودع غير مستخدم فعليًا (فرصة تنظيف).

---

# Navigation

- **الوصول:** الشريط الجانبي → مجموعة "المخزون" → المستودعات.
- **الداخل:** التبويبات الثلاثة + Dialogs العمليات.
- **التكامل:** المستودعات تظهر كمحدّد في المنتجات، إنشاء الطلب، واستلام أوامر الشراء.

---

# User Actions

| الإجراء | النوع | الثقة |
|--------|------|------|
| إضافة مستودع | كتابة | ✅ |
| تعديل مستودع | كتابة | 🟡 |
| أرشفة مستودع | كتابة | ✅ |
| تعيين افتراضي | كتابة | ✅ |
| تحويل مخزون | كتابة (تؤثر على المخزون) | ✅ |
| عرض سجل التحويلات | قراءة | ✅ |
| تبديل التبويبات | قراءة | ✅ |

---

# Buttons

| الزر | الموقع | الثقة |
|-----|--------|------|
| إضافة مستودع | الشريط العلوي | ✅ |
| تحويل مخزون | الشريط/صف المستودع | ✅ |
| تعيين كافتراضي / أرشفة / تعديل | صف المستودع | ✅/🟡 |
| تنفيذ التحويل / إلغاء | Dialog التحويل | ✅ |

---

# Forms

## نموذج إضافة/تعديل مستودع ✅/🟡
الاسم، العنوان، (مرجّح) تعيين كافتراضي.

## نموذج تحويل المخزون ✅
المستودع المصدر، المستودع الهدف، المنتج/المنتجات، الكمية لكل منتج.

---

# Tables

| الجدول | الأعمدة | الثقة |
|-------|---------|------|
| المستودعات | الاسم · العنوان · عدد المنتجات · الافتراضي · الإجراءات | ✅ |
| سجل التحويلات | من · إلى · التاريخ · البنود/الكميات | ✅/🟡 |

---

# Cards

- مرجّح عرض بطاقي بديل لكل مستودع (اسم + عدد منتجات + إجراءات) 🟡.

---

# Charts

- **لا رسوم بيانية** في الوحدة (تفصيل تكاليف المخزون حسب المستودع في `/analytics` → تبويب المخزون). ✅

---

# Filters

- التصفية الأساسية عبر **التبويبات** (نشط/مؤرشف). ✅
- لا فلاتر إضافية مرصودة 🟡.

---

# Search

- بحث في المستودعات: غير مؤكد (العدد صغير) 🟡.

---

# Sorting

- ترتيب المستودعات: مرجّح الافتراضي أولًا ثم الاسم/الإنشاء 🟡.
- سجل التحويلات: `created_at` تنازلي 🟡.

---

# Empty States

| الحالة | العرض المتوقّع | الثقة |
|-------|------------------|------|
| مستودع واحد فقط | لا تبويب مؤرشف/تحويلات فعّال | 🟡 |
| لا تحويلات | "لا تحويلات بعد" | 🟡 |
| لا مؤرشفة | تبويب المؤرشفة فارغ | 🟡 |

---

# Loading States

- تحميل قائمة المستودعات + عدّاد المنتجات لكل منها 🟡.
- تنفيذ التحويل: مؤشر أثناء تحديث المخزون في المستودعين 🟡.

---

# Error States

| الخطأ | السلوك | الثقة |
|------|--------|------|
| تحويل كمية أكبر من المتاح | رفض + رسالة | 🟡 |
| أرشفة مستودع افتراضي | يجب منعها/طلب تعيين افتراضي بديل | 🟡 |
| فشل التحويل جزئيًا | يجب أن يكون ذريًا (كله أو لا شيء) | ⚪ |

---

# Validation Rules

> التفصيل في `18_Business_Logic.md`.

| القاعدة | الوصف | الثقة |
|--------|-------|------|
| اسم المستودع | إلزامي | ✅ |
| مستودع افتراضي واحد | واحد فقط لكل شركة في أي وقت | 🟡 |
| كمية التحويل | ≤ المتاح في المصدر (`on_hand`) | 🟡 |
| المصدر ≠ الهدف | لا تحويل لنفس المستودع | ⚪ |
| منع أرشفة الافتراضي | إلا بعد تعيين بديل | 🟡 |

---

# Permissions

> المصفوفة الكاملة في `15_Permissions.md`.

- **مدير المستودع:** الوحدة مجاله الأساسي — وصول كامل للمستودعات والمخزون (بلا وصول للطلبات) ✅.
- **الأدوار كاملة الصلاحية:** وصول كامل ✅.
- **باقي الأدوار التشغيلية:** مرجّح قراءة أو لا وصول 🟡.

---

# Business Rules

> التفصيل في `18_Business_Logic.md`.

1. **المخزون منسوب لمستودع** عبر `inventory_stock` (لا مخزون عام). ✅
2. **مستودع افتراضي واحد** يُستخدم تلقائيًا في الطلبات/الاستلام. ✅
3. **التحويل عملية ذرية:** خصم المصدر + زيادة الهدف معًا، ويُسجَّل في `stock_transfers`. ✅
4. **الأرشفة لا تحذف:** المستودع المؤرشف يحتفظ بتاريخه، ويُخفى من الاختيارات النشطة. ✅
5. **التحويل لا يغيّر إجمالي المخزون** عبر الشركة، فقط توزيعه. ✅

---

# Workflow

```
إنشاء مستودع → تعيين افتراضي (اختياري)
   → استلام مخزون (من أوامر الشراء) في مستودع
   → تحويل بين المستودعات حسب الحاجة (stock_transfers)
   → التجهيز يخصم من مستودع الطلب
   → إيقاف مستودع → أرشفة (بعد تفريغ/تعيين بديل)
```

---

# Internal Logic

- **عدّاد منتجات المستودع:** `GET inventories?select=*,inventory_stock(id)` — عدّ صفوف المخزون لكل مستودع ✅.
- **سجل التحويلات:** `GET stock_transfers?select=*,from_inventory:inventories!from_inventory_id(name),to_inventory:inventories!to_inventory_id(name)` ✅ (ربط مزدوج لنفس الجدول).
- **تنفيذ التحويل:** مرجّح عبر RPC/معاملة تضمن ذرية الخصم/الزيادة 🟡.

---

# State Changes

| الحدث | الأثر | الثقة |
|------|------|------|
| تحويل مخزون | `on_hand`− (مصدر) + `on_hand`+ (هدف) + صف `stock_transfers` | ✅ |
| تعيين افتراضي | `is_default` ينتقل لمستودع واحد | ✅ |
| أرشفة | `is_archived = true` | ✅ |
| استلام أمر شراء | `on_hand`+ في المستودع المستهدف | ✅ |

---

# Possible Database Tables

| الجدول | الدور | الثقة |
|-------|------|------|
| `inventories` | المستودعات (المواقع) | ✅ |
| `inventory_stock` | المخزون لكل (منتج × مستودع) | ✅ |
| `stock_transfers` | رؤوس التحويلات | ✅ |
| `stock_transfer_items` | بنود التحويل (منتج + كمية) | 🟡 |
| `products` | المنتجات المحوّلة | ✅ |

---

# Possible Database Fields

**`inventories` ✅/🟡:**
`id`, `company_id`, `name`, `address`🟡, `is_default`, `is_archived`🟡, `created_at`.

**`inventory_stock` ✅:**
`product_id`, `inventory_id`, `on_hand`, `committed`, `quantity`. **PK مرجّح:** `(inventory_id, product_id)`.

**`stock_transfers` ✅:**
`id`, `company_id`, `from_inventory_id`, `to_inventory_id`, `created_at`.

**`stock_transfer_items` (مرجّح) 🟡:**
`id`, `stock_transfer_id`, `product_id`, `quantity`.

---

# Relationships

```
inventories (M) >── (1) companies              [company_id]
inventories (1) ──< inventory_stock >── (M) products   [المخزون]
stock_transfers (M) >── (1) inventories        [from_inventory_id]
stock_transfers (M) >── (1) inventories        [to_inventory_id]
stock_transfers (1) ──< stock_transfer_items >── (1) products   [بنود التحويل — مرجّح]
```

**ملاحظة (✅):** `stock_transfers` يرتبط بجدول `inventories` **مرتين** (مصدر وهدف) — ربط مزدوج لنفس الجدول، مؤكد من استعلام `from_inventory:...!from_inventory_id` و`to_inventory:...!to_inventory_id`.

---

# Possible APIs

> التفصيل الكامل في `13_APIs.md`.

| الغرض | الطلب | الثقة |
|------|-------|------|
| قائمة المستودعات + عدّاد | `GET /rest/v1/inventories?select=*,inventory_stock(id)` | ✅ |
| سجل التحويلات | `GET /rest/v1/stock_transfers?select=*,from_inventory:inventories!from_inventory_id(name),to_inventory:inventories!to_inventory_id(name)` | ✅ |
| إضافة مستودع | `POST /rest/v1/inventories` | 🟡 |
| تعديل/أرشفة/افتراضي | `PATCH /rest/v1/inventories?id=eq.` | 🟡 |
| تنفيذ تحويل | RPC/معاملة تحدّث inventory_stock + تنشئ stock_transfers | 🟡 |

---

# Request Flow

```
فتح /warehouses (تبويب النشطة)
  → GET inventories?select=*,inventory_stock(id) (المستودعات + عدد المنتجات)
تبويب سجل التحويلات
  → GET stock_transfers (مع ربط أسماء المصدر/الهدف)
تنفيذ تحويل
  → RPC/معاملة: خصم on_hand من المصدر + زيادة الهدف + إنشاء stock_transfers (+items)
  → تحديث القوائم
```

---

# Response Flow

- المستودعات تعود مع مصفوفة `inventory_stock(id)` لعدّها.
- التحويلات تعود مع أسماء المستودعين (المصدر/الهدف) مضمّنة.

---

# Performance Considerations

- عدد المستودعات صغير (3) → لا ضغط في القائمة.
- **عدّاد المنتجات لكل مستودع** عبر جلب صفوف `inventory_stock(id)` — قد يكبر للمستودع الرئيسي (561) → مرجّح استخدام count بدل جلب المعرّفات 🟡.
- سجل التحويلات ينمو بمرور الوقت → يحتاج ترقيمًا مستقبلًا 🟡.

---

# Security Considerations

- عزل `company_id` + RLS 🟡.
- **مدير المستودع** يجب أن يقتصر على مستودعات شركته 🟡.
- التحويل يجب ألا يتجاوز حدود الشركة (كلا المستودعين لنفس `company_id`) ⚪.

---

# Edge Cases

| الحالة | السلوك | الثقة |
|-------|--------|------|
| مستودع بـ 0 منتج (مصنع سيد) | صالح، قابل للأرشفة | ✅ |
| تحويل يفرّغ مستودعًا | صالح | ✅ |
| أرشفة الافتراضي | منع/طلب بديل | 🟡 |
| تحويل كمية غير متوفرة | رفض | 🟡 |
| تحويل منتج له committed | يجب مراعاة المتاح لا on_hand الكامل | ⚪ |

---

# Failure Scenarios

- **فشل نصف التحويل** (خصم دون زيادة): يفسد المخزون → يجب ذرية كاملة (RPC/معاملة) ⚪.
- **أرشفة مستودع له مخزون:** فقدان رؤية مخزون نشط → يجب تنبيه/منع 🟡.
- **حذف مستودع مرجعي:** كسر مراجع الطلبات/التحويلات → أرشفة لا حذف ✅.

---

# Logging Requirements

- كل تحويل مسجّل في `stock_transfers` (سجل دائم) ✅.
- الأرشفة/تغيير الافتراضي يجب تسجيلهما ⚪.

---

# Audit Requirements

- **سجل التحويلات هو سجل تدقيق فعلي** لحركة المخزون بين المواقع ✅.
- تدقيق من نفّذ التحويل (المستخدم) — مرجّح عبر عمود `created_by` ⚪ (لم يُرصد).

---

# Analytics Events

أحداث مرجّحة 🟡:
`warehouse_created`, `warehouse_edited`, `warehouse_archived`, `warehouse_set_default`, `stock_transfer_created`, `warehouse_tab_switched`.

---

# Technical Notes

- **الربط المزدوج لـ `stock_transfers` بـ `inventories`** (from/to) نمط تصميمي مهم يجب دعمه في القاعدة (مفتاحان أجنبيان لنفس الجدول).
- **`stock_transfer_items` مرجّح لكنه ضروري** إن كان التحويل يدعم عدة منتجات في عملية واحدة (وهو المتوقّع).
- **التحويل يجب أن يكون ذريًا** — أهم قاعدة تقنية هنا؛ فشل جزئي يفسد سلامة المخزون.
- **`is_default`** يجب فرض تفرّده (مستودع افتراضي واحد) على مستوى القاعدة ⚪.

---

# Reverse Engineering Notes

- التبويبات الثلاثة والعمليات (إضافة/تحويل/أرشفة/افتراضي) **مؤكدة ✅** (مشاهدة مباشرة).
- أعداد المنتجات لكل مستودع (561/3/0) **مؤكدة ✅**.
- استعلام سجل التحويلات بالربط المزدوج **مؤكد ✅** من الشبكة.
- `stock_transfer_items` (بنود التحويل) **مرجّح 🟡** — لم يُرصد مباشرة لكنه ضروري لتحويل متعدد المنتجات.
- ذرية التحويل عبر RPC **مرجّحة 🟡** (منطقية لكن غير مرصودة الآلية).
- عمود `created_by` للتدقيق **غير مرصود ⚪**.

---

# Confidence Level

| العنصر | الثقة |
|-------|------|
| التبويبات الثلاثة والعمليات | ✅ مؤكد |
| أعداد المنتجات لكل مستودع | ✅ مؤكد |
| جدول inventories + inventory_stock | ✅ مؤكد |
| stock_transfers + الربط المزدوج | ✅ مؤكد |
| stock_transfer_items (بنود التحويل) | 🟡 مرجّح |
| ذرية التحويل (RPC/معاملة) | 🟡 مرجّح |
| قواعد الافتراضي/الأرشفة | 🟡 مرجّح |
| أحداث التتبّع | 🟡 مرجّح |

---

READY FOR NEXT FILE
